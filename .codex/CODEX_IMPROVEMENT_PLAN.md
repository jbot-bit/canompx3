# Codex Improvement Plan

Status: closed 2026-05-25.

The Codex adapter backlog is intentionally empty. New Codex work should be
opened only when there is a measured failure, repeated operator friction, or a
specific user request.

## Closed Scope

- Repo-local Codex skills exist for workspace startup, verification, audit,
  live audit, deploy readiness, research scrutiny, and capital review.
- Codex launcher surfaces use the WSL-home path, session preflight, shared
  Codex home discovery, repo-state/research-catalog grounding, and the
  `canompx3_power` heavy review profile.
- The local Codex doctor treats `gpt-5.5` as the current primary default after
  verifying the installed CLI and recent local log use that model.
- Optional Codex automation remains report-only. There is no open automation
  build task.

## Reopen Rule

Do not keep speculative Codex backlog in this file. If a future issue appears,
fix it directly or create a short, dated stage/plan with measured evidence.
