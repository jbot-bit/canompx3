# Pip install constraints for sidecar deps not managed by uv.lock.
#
# Use:  pip install -c constraints.txt <package>
#
# cryptography<47 — Authlib 1.7.0 (latest as of 2026-04-29) still imports
# `cryptography.hazmat.backends.default_backend`, which was removed in
# cryptography 47.0.0. fastmcp depends on authlib.jose, so any FastMCP-based
# MCP server (code-review-graph, gold-db) crashes on startup with cryptography>=47.
# Drift check #17 (`check_cryptography_pin_holds`) fail-closes on regression.
#
# revisit-by: 2026-10-29
# When this date passes, drift check emits ADVISORY. To resolve:
#   1. Check Authlib release notes — has the hazmat.backends import been removed?
#   2. If still broken: bump revisit-by forward 180 days.
#   3. If fixed: remove the cryptography<47 line, re-test MCPs (`/mcp` connects).
# Detail: memory/feedback_mcp_venv_drift_cryptography47.md
cryptography<47

# fastmcp — runtime dep of trading_app/mcp_server.py (the gold-db MCP server).
# Deliberately NOT in pyproject.toml / uv.lock: pulling fastmcp into the locked
# dep tree drags cryptography + authlib + ~30 transitive deps into every uv sync,
# polluting the production runtime for a sidecar tool. Treated as sidecar infra,
# same pattern as code-review-graph (per CLAUDE.md § MCP Server).
#
# If gold-db MCP fails to connect with `ModuleNotFoundError: No module named 'fastmcp'`:
#   .venv/Scripts/python.exe -m pip install -c constraints.txt fastmcp
# Then `/mcp` (Claude Code) → confirm "gold-db: ✓ Connected".
# Detail: memory/feedback_mcp_partial_install_state_2026_05_01.md
fastmcp
