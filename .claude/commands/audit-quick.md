Run the quick audit (phases 0, 1, 3, 6 only). No arguments needed.

Use when: "quick audit", "fast audit", "quick check", "abbreviated audit"

```bash
.venv/bin/python scripts/audits/run_all.py --quick
```

Quick mode covers: triage, automated checks, docs, and build chain staleness.
