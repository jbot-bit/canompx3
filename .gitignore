# Hook runtime state
.claude/hooks/.data-first-state.json

# Database
gold.db
gold.db.wal
local_db/

# Generated trade sheets
trade_sheet.html
promotion_candidates.html

# Pulse (regenerated on demand)
.pulse_cache.json
.pulse_last_session.json
PROJECT_PULSE.md

# Vector DB cache (Chroma)
chroma/
chroma_data/

# DB parquet exports (generated artifacts)
db_export/

# Data files (large, not for VCS)
DB/
OHLCV_MGC_FULL/
dbn/
*.dbn
*.dbn.zst
*.zip

# Backups
backups/
gold.db.bak*

# Generated tools
pipeline-explorer.html
dashboard.html

# Generated outputs (paper trade, strategy exports)
paper_trade_*.csv
APEX_PHASE1_*.txt
apex_phase1_*.csv
gold_ro.db

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
.venv-wsl/
env/

# Logs & checkpoints
*.log
logs/traces/*.json
pipeline/checkpoints/

# Environment & secrets
.env
.env.*

# Live session runtime output
live_signals.jsonl

# Serena (project-local state, memories, index)
.serena/

# IDE
# .vscode/  — settings.json committed for team consistency
.idea/
*.swp
*.swo

# Claude Code (ignore internals, track rules/commands)
.claude/*
!.claude/rules/
!.claude/skills/
!.claude/hooks/
!.claude/agents/
!.claude/settings.json
.claude/hooks/.last_drift_ok
memory/

# Windows junk
nul
Thumbs.db
Desktop.ini

# Obsidian
.obsidian/

# External tools (separate repos)
openclaw/
.openclaw/
llm-code-scanner/

# OpenClaw-generated identity/config files
AGENTS.md
BOOTSTRAP.md
HEARTBEAT.md
IDENTITY.md
SOUL.md
TOOLS.md
USER.md

# TradingView exports
*.xlsx
COMEX_*.csv
DT_V2_*.csv

# MNQ scratch
MNQ db/

# Reports (generated analysis output)
reports/
outputs/

# Walk-forward results (JSONL output)
data/

# Research output (generated analysis artifacts)
research/output/

# Session artifacts (accidental shell stubs, temp notes)
bash
cd
git
tasks.txt
*.tmp
_tmp_*.py
tmp_*.py

# Working/temp docs (prompts, plans, scratch notes)
PLAN.md
PLAN_*.md
PROMPT_*.md
E3_REBUILD_PROMPT.md
DB_PERFORMANCE_PLAN.md
*.docx
orbresearch.txt
live_journal.db
mlprompt*.txt

# External repos cloned into project tree
CodePilot/

# Outcome builder heartbeat (runtime artifact)
outcome_builder.heartbeat

# Misc
backfill_overnight.log

# Auto Claude data directory
.auto-claude/

# ML model artifacts (regenerable via: python -m trading_app.ml.meta_label --all)
models/ml/*.joblib

# Pinecone Assistant sync state + generated upload files
scripts/tools/.pinecone_assistant_id
scripts/tools/.pinecone_sync_manifest.json
scripts/tools/_snapshot_*.md
scripts/tools/_bundle_*.md
scripts/tools/_living_*.md
scripts/tools/_clean_*.md

# Auto Claude generated files
.auto-claude-security.json
.auto-claude-status
.claude_settings.json
.worktrees/
.security-key
logs/security/

# Tool caches (firecrawl, claire worktrees, notebooklm config)
.claire/
.firecrawl/
notebooklm-config.json
parameter_stability.html

# Pinecone sync state
scripts/tools/.pinecone_sync_state.json

# uv lockfile — COMMITTED for reproducible builds
# uv.lock

# Cloudflare tunnel credentials (never commit)
.cloudflared/*.json
.cloudflared/*.pem

# Shell dotfiles (leaked from home dir)
.bash_profile
.bashrc
.zshrc
.zprofile
.profile
.gitconfig
.gitmodules
.ripgreprc

# Git internals leaked into worktree root
hooks/
objects/
refs/

# Venv path artifacts
..venv*

# Temp path artifacts (mangled Windows paths)
C*/
Users*

# Config dir (hypercorn/secret_key — not for VCS)
config/

# PDF resources (large, not for VCS)
resources/*.pdf

# Temp script output
scripts/tmp_lit_results.txt

# WSL env scripts
scripts/infra/wsl-env.ps1
deets.txt
