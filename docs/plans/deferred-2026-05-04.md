# Deferred Decisions — 2026-05-04 Cleanup Session

Branches and PRs that survived the cleanup pass with substantive content but no clear owner-decision. Listed so the next session doesn't have to rediscover what they are.

---

## Active Codex work — leave alone

### `codex/topstep-operator-arch-v2` (tip `920bccb2`, in `C:\Users\joshd\canompx3`)
- The canonical Codex working branch. Currently has the empty-tree wipe restore (`7c05a60b`) and the allocator paused-pool diagnosis (`955eb980`).
- Codex session was active during this cleanup; now context-cleared.
- **Decision needed:** none right now. Codex will pick this up again.

### `codex/deepseek-openrouter-runtime-v2` (tip `a17c9918`, last commit 2026-05-04 14:29)
- 13 commits, +4441 lines: bounded openrouter research runtime + topstep operator export contract surface.
- The other terminal's audit critiqued this branch (provider_registry / research_packet shortfalls).
- **Decision needed:** triage the audit findings against the branch (audit suggested: build eval harness, implement structured_extraction properly, add capability checks). Probably wants its own focused session.

---

## Substantive feature branches — your eyeball needed

### `codex/passive-sidecar-skeleton` (tip `a4e9f362`, last commit 2026-05-02)
- **27 files / +2125 lines / -12 lines** — a full passive-sidecar feature skeleton:
  - `trading_app/live/passive_sidecar/` — `data_consumer.py` (168), `alert_engine.py` (174), `policy_gate.py`, `dashboard_feed.py`, `__init__.py`, `README.md`
  - `tests/test_trading_app/test_passive_sidecar.py` (97)
  - `scripts/check_passive_sidecar_non_exec.py` (60) + test (53)
  - `.githooks/pre-commit` step added
  - 11 docs in `docs/operator/` + `docs/plans/active/2026-05/` + `docs/research/`
  - Touches `trading_app/live/bot_state.py` (+83), `session_orchestrator.py` (+68), `prop_profiles.py` (+8)
- **Decision needed:** ship as PR, or close (work parked, no current need)?
  - **Path A (ship):** would land a real-money-adjacent guardrail (passive-sidecar non-executing audit lane + alert engine). Worth a careful review.
  - **Path B (park):** if the operator architecture moved past this design (Codex did keep iterating into `topstep-operator-arch-v2` and `topstep-operator-v3` afterward), the skeleton may be superseded. Closing-with-pointer preserves the branch.
- **First action when you decide:** read `docs/operator/passive-sidecar-enforcement.md` (56 lines) + `trading_app/live/passive_sidecar/README.md` to know whether the design is still current.

---

## Older feature branches that already have open PRs — your call

### PR #206 — `fix/drift-checks-fail-quiet-class`
- 2026-05-01, real guardrail fix. CI green at last check.
- **Decision:** review and merge when you have 30 minutes.

### PR #205 — `ship/strategy-lab-mcp`
- Was stacked on PR #203 (now merged 2026-05-04). Branch base needs rebase to main.
- GitHub's auto-update-branch refused (real merge conflict between base and head).
- **Decision:** rebase locally, push, re-check CI. Or close if strategy-lab MCP isn't a near-term need.

### PR #210 — `codex/create-plan-to-tidy-up-repository` (the cleanup playbook itself)
- CI was failing because PR pre-dated the X_MES_ATR60 test fix (PR #212).
- Branch was updated from main during this session (head `dcba5fd5`); CI rerunning.
- **Decision:** wait for CI; if green, merge.

### PR #214 — `validator-honesty-fix`
- Your active work. Not stale.

### PR #215, #216 — opened during this session
- #215: prior-day-context blocker memo cherry-pick (rescued from `wt-codex-parallel-20260502-051616`).
- #216: GitNexus eval doc cherry-pick (rescued from `tooling/gitnexus-eval`).
- Both doc-only, doc-only-CI, should auto-pass.

---

## Stash residue — gone, don't re-look

All 8 stashes resolved in this session:
- 4 dropped (3 stale HANDOFF.md edits + 1 three-line workflow rule).
- 1 saved as branch `wip/launch-lock-no-clone` (172-line `acquire_launch_lock` cross-worktree mutex in `pipeline/system_context.py`). NOT yet shipped — convert to PR if you want the launch-lock guardrail.
- 3 dropped because content already landed in main via earlier PRs (#141, #180, etc.).

---

## Tracking-only — branches that exist but have content already in main or shipped via other PRs

- `chore/handoff-2026-05-01-closeout` — PR was #196, closed in this session as superseded.
- `phase-d-volume-pilot-d0` — Phase D research branch, has a tracking PR somewhere; leave for the daily-append work.
- `wt-codex-mnq-hiroi-scan` — research findings already landed via chordia audit series; PR was #99, closed.
- `tooling/seq-thinking-mcp-eval` — PR was #193, closed (eval done, no install).
- `tooling/git-mcp-eval` — PR #191 MERGED 2026-05-04. Branch may be auto-deleted.
- `ship/codex-supercharge-roadmap` — PR was #203 MERGED 2026-05-04. Branch may be auto-deleted.
- `fix/orb-size-stats-3tuple-callers` — PR #204 MERGED 2026-05-04. Branch may be auto-deleted.
- `ship/codex-local-plugin` — appears to share tip with `ship/strategy-lab-mcp`; check before deleting.

---

## Summary numbers

Before cleanup: 12 open PRs, 8 stashes, 24 local branches, 4 worktrees (1 dead).
After cleanup: 5 open PRs (2 newly opened for memo rescues), 0 stashes, 18 local branches, 3 worktrees.
Closed without merge (preserved on branch): #99, #193, #196, #208, #209.
Merged: #191, #203, #204 (203/204 by external session, 191 by this cleanup).
Opened: #215 (prior-day memo rescue), #216 (gitnexus eval archive).
