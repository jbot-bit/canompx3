# Decision Ledger

Use this file for durable accepted decisions that should survive handoff churn.

## Current

- `topstep-mnq-single-copy-3-lane-pilot-2026-05-30` - The immediate
  `topstep_50k_mnq_auto` live-pilot authority is MNQ-only, effective
  `--copies 1`, and exactly three active lanes:
  `MNQ_COMEX_SETTLE_E2_RR1.5_CB1_OVNRNG_100`,
  `MNQ_TOKYO_OPEN_E2_RR1.5_CB1_COST_LT08`, and
  `MNQ_US_DATA_1000_E2_RR1.5_CB1_VWAP_MID_ALIGNED_O15`.
  `MNQ_NYSE_OPEN_E2_RR1.0_CB1_COST_LT12` is parked from the active allocator
  book for the pilot because current SR state is `ALARM`; the lane is not
  deleted, replaced, or re-tuned. Re-entry requires a separate prereg/re-audit
  package with fresh canonical SR evidence, no active pause, and explicit
  approval. Profile telemetry maturity is advisory for this express/funded
  profile, not a hard live blocker; multi-copy live remains blocked until
  per-shadow software daily-loss/HWM belts exist.

- `track-d-gate0-runner-table-contract-2026-05-29` - Track D Gate 0 design
  blocker is closed at the contract level. Current checkout was refreshed
  against `origin/main` before authoring; `gold.db` read-only measurement found
  1,741 exact `MNQ COMEX_SETTLE O5 E2 RR1.5 CB1` candidate windows
  (1,658 IS before 2026-01-01, 83 OOS from 2026-01-01), with minute-resolution
  `entry_ts`. Databento metadata-only probes showed `GLBX.MDP3` `tbbo` and
  `mbp-1` availability across the required date range and representative
  five-window costs of $0.002799630165 (`tbbo`) and $0.007555380464 (`mbp-1`).
  Accepted design contract:
  `docs/plans/2026-05-29-track-d-gate0-runner-table-contract.md`. No data
  download, schema mutation, validated_setups mutation, lane allocation, broker
  state, live config, or paper-trade carrier. Future implementation must start
  with manifest build and full metadata-cost estimate, guarded by explicit
  `--max-cost-usd`; MBO remains out of scope unless top-of-book Gate 0 passes
  and leaves a specific explanatory gap.

- `validated-setups-strict-is-window-scope-2026-05-21` — `StrategyTradeWindowResolver` now takes `holdout_cutoff` kwarg. Refresh path (`backfill_validated_trade_windows.py`) + Check 45 (`check_active_native_trade_windows_match_provenance`) call with `HOLDOUT_SACRED_FROM`; promotion path (`strategy_validator.py`) and `check_active_micro_only_filters_after_micro_launch` keep full-window (default `None`). One-time migration ran live against `gold.db`: 844 of 844 active VALIDATOR_NATIVE rows updated — window columns (`first_trade_day`, `last_trade_day`, `trade_day_count`) now pair to the same strict-IS population (`trading_day < 2026-01-01`) that `sample_size` / `expectancy_r` / `win_rate` / `sharpe_ratio` were frozen on at promotion-time. Doctrine: `validated_setups` is a strict-IS provenance shelf for VALIDATOR_NATIVE rows, NOT a live trailing-performance view. Pre-snapshot retained at `gold.db.bak.pre-strict-is-window-2026-05-21`. Check 45 = 0 violations post-refresh. Files: `trading_app/validation_provenance.py`, `trading_app/chordia.py`, `scripts/migrations/backfill_validated_trade_windows.py`, `pipeline/check_drift.py`. Tests deferred (context cap; capture as follow-up: 3 new cases per design proposal).
- `validated-setups-trade-window-refresh-2026-05-21` — Scheduled data refresh of `validated_setups` active native rows against canonical `orb_outcomes` through 2026-05-19. Stage file: `docs/runtime/stages/2026-05-21-validated-setups-refresh.md` (deleted on close). Pre-snapshot retained at `C:/Users/joshd/canompx3/gold.db.bak.pre-validated-setups-refresh-2026-05-21`. Before: 844 active rows (MNQ=783, MES=48, MGC=13) at max_last_trade_day ∈ {2026-05-14, 2026-05-15}. After: 844 active rows (unchanged), max_last_trade_day=2026-05-19 for all instruments. Row count unchanged; 0 inserted, 0 deleted. `docs/runtime/lane_allocation.json` byte-identical pre/post (last touch HEAD f1ece74f, refresh did not mutate). No live config / chordia_audit_log mutation. `gold.db` is in `.gitignore` so no git commit of the DB itself; this ledger entry is the durable record of the refresh. Open caveat: `sum(sample_size)` over the active cohort was identical pre/post (813614), which suggests the validator writer advanced `last_trade_day` but did not recompute `sample_size` from the 1-7 newly-available trading days — verify writer contract before next refresh.
- `fast-lane-promote-threshold-parity-drift-check-landed-2026-05-19` —
  Added `check_fast_lane_promote_threshold_parity` (Check #158, registered in
  `CHECKS` list of `pipeline/check_drift.py`). Asserts the six gated constants
  in `scripts/research/fast_lane_promote_queue.py` (T_KILL_FLOOR=2.5,
  T_PROMOTE_FLOOR=3.0, EXPR_FLOOR=0.0, N_FLOOR=50, FIRE_MIN=0.05, FIRE_MAX=0.95)
  match the canonical YAML at `docs/audit/hypotheses/TEMPLATE-fast-lane-v5.1.yaml`
  `screen:` block (lines 102-115). Closes Layer 1 of the 3-layer canonical-inline-copy
  hardening — this is the 4th confirmed instance of the
  `canonical-inline-copy-parity-bug-class` (see
  `memory/feedback_canonical_inline_copy_parity_bug_class.md`). 11 mutation-probe
  injection tests in `tests/test_pipeline/test_check_drift_fast_lane_promote_threshold_parity.py`
  cover: clean-state pass, missing-template fail-closed, per-constant drift for
  T_KILL_FLOOR / T_PROMOTE_FLOOR / EXPR_FLOOR / N_FLOOR / FIRE_MIN / FIRE_MAX
  (sibling coverage per `regex-alternation-sibling-coverage`), template-side
  amend drift, and null / non-numeric canonical-value fail-closed structural
  probes. (Original count was 8; EXPR_FLOOR + null/non-numeric tests added in
  the evidence-auditor follow-up.) Drift count 157 → 158. Layer 2 (`CANONICAL_INLINE_COPIES` meta-registry)
  + Layer 3 (PreToolUse hook flagging new inline literals) PARKED for fresh-context
  design proposal.

- `mnq-comexsettle-orbvol16k-fast-lane-promote-revoked-2026-05-18` —
  **REVOKED FAST_LANE PROMOTE on lane `MNQ_COMEX_SETTLE_E2_RR2.0_CB1_ORB_VOL_16K`.**
  Pooled t=3.30 was a sample-doubling artifact. Per-direction breakdown
  already in the result MD (long t=2.12 N=47, short t=2.53 N=50) shows both
  directions KILL_AS_STANDALONE under v5.1 thresholds (t<2.5 floor AND
  fire-rate <5% extreme on both sides). Heavyweight Chordia authoring is
  NOT authorized. Sibling cells (other RR/CB/aperture/session/instrument
  variants) are out of scope of this revocation. Evidence:
  `docs/audit/results/2026-05-18-mnq-comexsettle-e2-rr20-cb1-orbvol16k-pooled-o5-fast-lane-v1.revocation.md`.
  Scanner status: REVOKED. Spent K: 0 (re-inspection of existing emissions).

- `fast-lane-promote-queue-scanner-landed-2026-05-18` —
  Built `scripts/research/fast_lane_promote_queue.py` (read-only scanner +
  derived-state cache `docs/runtime/promote_queue.yaml`) and drift check
  `check_fast_lane_promote_orphans` (Check #157, registered in `CHECKS` list
  of `pipeline/check_drift.py`). Closes the missing-pipe gap between
  FAST_LANE v5.1 PROMOTE results and follow-up. Status enum is exhaustive
  (QUEUED / ESCALATED / REVOKED / PARKED / ERROR — no UNKNOWN). Per-direction
  sanity gate catches pooling-inflation artifacts before heavyweight K is
  spent. Promote_queue.yaml is derived state — drift check reconstructs
  from on-disk artifacts on every run.

- `live-broker-http-client-canonical-2026-05-18` — `trading_app/live/http_client.py`
  (`BrokerHTTPClient`) is the **single sanctioned HTTP surface** for broker
  endpoints. Every call into ProjectX or Tradovate REST APIs MUST route through
  it. The client carries (a) the bounded retry budget, (b) idempotency keys for
  POSTs that mutate broker state, and (c) the `failure_hook` that drives the
  orchestrator's `CircuitBreaker` (wired Stage 4, commit `1bab44c7`). Direct
  `requests.get` / `requests.post` / `requests.request` / `requests.put` /
  `requests.delete` / `requests.patch` calls inside
  `trading_app/live/projectx/` or `trading_app/live/tradovate/` are forbidden —
  enforcement is **Check 156** `check_no_direct_requests_to_broker_endpoints`
  in `pipeline/check_drift.py` (commit `f7c5189e`). The check is mutation-proof
  (proven by injection probe Stage 5 partial: literal `requests.get(...)`
  injected into `projectx/positions.py:25` → caught with file:line diagnostic →
  reverted → returned to PASS) and is reinforced by the regression test in
  `tests/test_pipeline/test_check_drift_broker_endpoints.py`. Allowlist scope:
  the canonical client itself (`trading_app/live/http_client.py`) and any
  module outside the two broker directories. Importing `requests` for type
  hints / exception classes (`requests.RequestException`) is permitted; only
  the call syntax is flagged. This decision closes the doctrine gap left at
  the end of Stages 1-2 of `feat/live-broker-resilience` (broker HTTP
  resilience baseline). Future broker adapters (Rithmic, etc.) inherit the
  canonical client and the failure_hook for free.

- `mnq-nyse-close-k1-prereg-blocked-by-loader-2026-05-17` —
  ⚠️ **SUPERSEDED → UNBLOCKED (Amendment 3.3, PR #292, commit `8ab4fe13`, 2026-05-17)**
  > Path (a) — the loader amendment recommended in the original entry —
  > landed as Amendment 3.3 (explicit `metadata.theory_grant: false` for
  > no-theory Pathway B, fail-closed loader). The locked prereg
  > `docs/audit/hypotheses/2026-05-13-mnq-nyse-close-mode-a-k1-revalidation.yaml`
  > already carries `theory_grant: false` + `testing_mode: individual` and
  > now loads cleanly under the post-3.3 loader (verified 2026-05-18:
  > `load_hypothesis_metadata` returns `has_theory=False`, `testing_mode=individual`,
  > `n_hypotheses=1`, `total_expected_trials=1`, sha `f6e1f97716cdf929…`).
  > The locked statistical hurdle (`t >= 3.79` strict no-theory Chordia)
  > is preserved — no chordia_threshold_basis flip required.
  > Runner `research/chordia_strict_unlock_v1.py:92` calls the same loader
  > path; the Stage 1 K=1 head is now executable. Cohort-park binding on
  > the 78 ROUTABLE_DORMANT cohort (`docs/audit/results/2026-05-17-deployment-coverage-orphans.md`)
  > can be released for the two MNQ NYSE_CLOSE rows once the K=1 head
  > runs and writes a verdict. Cross-refs: `feedback_chordia_theory_citation_field_presence_trap.md`
  > (still operative — theory_citation OMITTED on this prereg, no
  > downgrade risk), HANDOFF.md "Open carry-overs" entry (also being
  > updated this session).

  **Original entry (preserved for audit trail, 2026-05-17):**

  `docs/audit/hypotheses/2026-05-13-mnq-nyse-close-mode-a-k1-revalidation.yaml`
  (Stage 1 of the 3-stage NYSE_CLOSE triage in
  `docs/plans/2026-05-12-mnq-nyse-close-institutional-validation-pathway.md`) is
  **non-executable** due to a doctrine collision between
  `memory/feedback_chordia_theory_citation_field_presence_trap.md` (OMIT
  `theory_citation` entirely for strict no-theory `t>=3.79` Chordia preregs —
  any truthy value silently downgrades the hurdle to 3.00) and
  `trading_app/hypothesis_loader.py:288-295` (Amendment 3.0 requires
  `theory_citation` on EVERY hypothesis when `testing_mode: individual`).
  Runner `research/chordia_strict_unlock_v1.py` fails fast with
  `HypothesisLoaderError` at load time; zero writes to result/CSV/registry.
  Verdict on the 78 ROUTABLE_DORMANT cohort (`docs/audit/results/2026-05-17-deployment-coverage-orphans.md`)
  stays **BLOCK** — no activations, including the two MNQ NYSE_CLOSE rows
  (X_MES_ATR70 25.3R, ATR_P50 23.2R), since the cohort-park triage rule
  binds them to the K=1 head's verdict and the K=1 head cannot run.
  Unblock paths (any single one suffices, all out of scope this session):
  (a) loader amendment to allow `theory_citation: null` or sentinel
  `NONE_NO_THEORY_GRANT` for `testing_mode: individual` strict-3.79 cases,
  (b) prereg edit stamping a `theory_citation` value and flipping
  `chordia_threshold_basis` to `t>=3.00 with theory grant` (alters the
  locked statistical hurdle — requires explicit reauthorization since the
  prereg is `date_locked: 2026-05-13`),
  (c) reframe as `testing_mode: family` with `expected_trial_count: 1`
  (Bonferroni K=1) — different multiple-testing math, requires user
  sign-off and re-lock. Recommended next move is (a) via a design
  proposal under `docs/plans/` — lands the no-theory Pathway-B path
  cleanly for all future preregs, not just this one. No
  `lane_allocation.json` or `prop_profiles.py` mutation. Cross-ref:
  HANDOFF.md Next Step #2 (MNQ highest-EV branch).

- `c8-allocator-gate-resolution-2026-05-14` — **BUG verdict** (not DOCTRINE, not
  PARTIAL): the lane allocator never read `validated_setups.c8_oos_status`,
  so two MNQ lanes carrying `FAILED_RATIO` reached the proposed-rebalance set
  unchallenged (`OVNRNG_25` currently DEPLOYED, `ORB_VOL_8K` proposed ADD).
  Pre-implementation gate confirmed not-grandfather: OVNRNG_25 entered
  `lane_allocation.json` on commit `95fe44c3` (2026-05-14) — 20 days AFTER
  `c8_oos_status` writes landed at `6887632f` (2026-04-24). Fix mirrors PR #197
  chordia gate doctrine. New `apply_c8_gate()` in `trading_app/lane_allocator.py`
  demotes non-PASSED non-NULL labels to PAUSE (`FAILED_RATIO`, `NEGATIVE_OOS_EXPR`,
  `NO_OOS_DATA`, `INSUFFICIENT_N_PATHWAY_B_REJECT`,
  `INSUFFICIENT_N_PATHWAY_A_PASS_THROUGH`, `REJECTED`); NULL passes through as
  Phase-4 grandfather (matches validator's `strategy_validator.py:1778` SKIPPED
  NULL emission). `c8_oos_status` now surfaces in lane JSON, `paused[]`, and
  `stale[]`. Second-layer drift check #147 `check_lane_allocation_c8_gate`
  fails the build if any `lanes[]` entry carries a non-PASSED non-NULL c8 label.
  Ordering inside `build_allocation()`: chordia (C4) first, c8 (C8) second,
  live-tradeability last. 13 new unit tests + parity test verifying both gates
  fire under `build_allocation`; `TestC8Gate` covers every c8 label, the
  grandfather pass-through, the existing-PAUSE/STALE additive guard, and a
  defensive-unknown-label branch. Drift suite 132 checks PASSED. Lane impact:
  OVNRNG_25 and ORB_VOL_8K auto-PAUSE on the next rebalance; the
  +2.80 R/yr rebalance proposal that gated this decision collapses to the
  surviving c8-clean set. No live capital mutation in this commit
  (`lane_allocation.json` unchanged until next rebalance run). Closes
  action-queue P1 `c8_oos_status_allocator_doctrine_2026_05_14`; unblocks P2
  `lane_allocation_rebalance_2026_05_14_pending_capital_review_blockers`
  blocker (a).

- `o30-pass-chordia-audit-not-deployed-2026-05-14` — MNQ
  `US_DATA_1000_E2_RR1.0_CB1_VWAP_MID_ALIGNED_O30` cleared strict no-theory
  Chordia hurdle (t=4.450, N=866, ExpR=0.1332; clears 3.79 by 0.66) per
  bounded canonical replay (`research/chordia_strict_unlock_v1.py`) under
  pre-reg `docs/audit/hypotheses/2026-05-13-mnq-usdata1000-vwapmid-o30-rr10-chordia-unlock-v1.yaml`.
  Result MD `docs/audit/results/2026-05-13-mnq-usdata1000-vwapmid-o30-rr10-chordia-unlock-v1.md`;
  audit-log entry committed to canonical `docs/runtime/chordia_audit_log.yaml`
  on main as commit `2cf2c9cc`. Pre-reg + result MD + handoff dedup committed
  on `research/vwap-mid-aligned-o30-prereg` as `14711a06`, `4d62885a`,
  `f84261d4`. **Audit verdict independently verified SOUND** via end-to-end
  evidence trace: Chordia 2018 verbatim threshold quote
  (`docs/institutional/literature/chordia_et_al_2018_two_million_strategies.md:20`),
  loader field-presence trap logic
  (`trading_app/hypothesis_loader.py:267-268`),
  scratch policy (runner line 269 `fillna(0.0)`, no NULL drops in CSV; 167 IS
  scratches counted as R=0; the 5% delta vs `validated_setups.t=4.230` is the
  documented wins+losses-only-vs-wins+losses+scratches denominator difference,
  not inflation), independent CSV recompute matches t=4.450 to 3 decimals,
  cohort lower bound `WF_START_OVERRIDE["MNQ"] = date(2020, 1, 1)`
  (`trading_app/config.py:379`), K-budget gate PASS at N=1, no fresher
  KILL/NO-GO supersedes. **Allocator dry-run on the same date shows O30 does
  NOT enter the deployed 4-slot set** (canonical
  `scripts/tools/rebalance_lanes.py --output <tmp>` to temp file, no live
  mutation): O30 is sibling-aperture variant of already-deployed
  `MNQ_US_DATA_1000_E2_RR1.0_CB1_VWAP_MID_ALIGNED_O15` and ranks below
  ORB_VOL_8K (annual_r 10.5) at the marginal slot. **O30 is bullpen
  replacement** if any deployed VWAP_MID_ALIGNED-family lane degrades. No
  capital state changed. Two action-queue items opened from this work:
  `c8_oos_status_allocator_doctrine_2026_05_14` (P1, blocking) and
  `lane_allocation_rebalance_2026_05_14_pending_capital_review_blockers` (P2,
  blocked-by C8). Capital-review verdict on the broader rebalance: VERIFY_MORE
  (live-control checks not exercised; kill/flatten not traced; SR-tripwire
  blind spot on all 3 NEW candidates; +2.80 R/yr below noise floor for a
  3-of-4 lane churn).

- `mnq-live-readiness-gap-fill-2026-05-11` — Live-readiness pass found and
  fixed a real blocker at the evidence-metadata layer, not by weakening proof.
  Canonical MNQ E2 TBBO routine-slippage evidence already covers 9 deployed
  MNQ sessions, but blank `validated_setups.slippage_validation_status` rows
  were hard-blocked as `slippage_missing`. `trading_app/deployability.py` now
  infers only `MNQ` + `E2` + covered-session rows to controlled
  `slippage_event_tail_pending`; uncovered sessions, MNQ E1, MES, MGC, and all
  other hard blockers still fail closed. Profile audit now shows 2/2 selected
  Topstep MNQ lanes as `CONTROLLED_LIVE_PILOT_CANDIDATE` with no hard blockers.
  All-active MNQ audit now completes normally and shows 177 controlled pilot
  candidates out of 786 active MNQ rows; these are shelf/profile-construction
  candidates, not direct live routes. Institutional language remains disallowed
  for all rows because event-tail / DSR / warning state remains. Broad audit
  native crash was also hardened by refreshing the read-only replay DuckDB
  connection every 50 rows. Result doc:
  `docs/audit/results/2026-05-11-mnq-live-readiness-gap-fill.md`. JSON outputs:
  `docs/audit/results/2026-05-11-topstep-50k-mnq-profile-deployability.json`,
  `docs/audit/results/2026-05-11-mnq-all-active-deployability.json`. No live
  allocation, broker, schema, or deployment DB state changed.

- `mnq-live-tradeability-lens-reset-2026-05-11` — Follow-up to the user’s
  tunnel-vision concern. Wider lens does **not** mean weaker proof or a broader
  undocumented search. It means changing the tested role: standalone signal vs
  allocator replacement/additivity vs execution/portfolio layer. The 2026-05-10
  proof gate closed the old five-row PRIORITY_A queue: two `PD_*` E2 rows are
  **WRONG as tested** under the correct live-placeable long-stop question; one
  NYSE row is **UNVERIFIED**; two COMEX rows were routed to a bounded
  additivity/replacement gate and both classified **PARK** on 2026-05-11.
  `X_MES_ATR60`: add improves annualized R/Sharpe but is same-session and
  correlation-rejected; replacement is -1.5 annualized R and -0.008 Sharpe.
  `ORB_G5`: add loses Sharpe, replacement loses Sharpe, and subset/correlation
  reject. Durable lens reset:
  `docs/audit/results/2026-05-11-mnq-live-tradeability-lens-reset.md`.
  Queue update: `allocator_paused_pool_priority_a_audits` closed and
  `mnq_live_tradeability_additivity_gate_2026_05_11` opened then closed with
  result doc `docs/audit/results/2026-05-11-mnq-comex-additivity-replacement-gate.md`.
  No live allocation, DB, broker, or schema state changed.

- `mnq-production-trade-expansion-proof-gate-2026-05-10` — User asked for more live-tradeable trades without tunnel vision. Fresh proof gate says **no new production lane from this pass**. `PD_GO_LONG` / `PD_CLEAR_LONG` E2 evidence is **WRONG as tested** for production because registered `PD_*` filters select on close-confirmed `orb_<session>_break_dir='long'`, which can be post-entry for E2 stop-market fills. Clean long-stop replay answered the correct live question: `MNQ_US_DATA_1000 PD_GO_LONG` clean IS t=1.885, OOS ExpR=+0.0049; `MNQ_COMEX_SETTLE PD_CLEAR_LONG` clean IS ExpR=-0.0192, t=-0.480. Code gates now block deployment-bound E2 use of `PD_*`: discovery skips it, execution refuses to arm it, deployability returns `NO_GO_BIAS_OR_DATA`, allocator pauses it. `COMEX_SETTLE X_MES_ATR60` remains **CONDITIONAL** and `COMEX_SETTLE ORB_G5 RR1.0` remains **CONDITIONAL parent/session exposure**, both requiring allocator replacement/additivity and runtime gates before any live expansion. Summary: `docs/audit/results/2026-05-10-mnq-production-trade-expansion-proof-gate.md`; clean replays: `docs/audit/results/2026-05-10-clean-long-stop-mnq-us-data-1000-e2-rr1.0-cb1-pd-go-long.md`, `docs/audit/results/2026-05-10-clean-long-stop-mnq-comex-settle-e2-rr1.0-cb1-pd-clear-long.md`. No `lane_allocation.json`, DB, or live-routing change.

- `chordia-unlock-nyseopen-rr15-costlt12-2026-05-07` — Bounded exact-lane Chordia unlock for `MNQ_NYSE_OPEN_E2_RR1.5_CB1_COST_LT12`. Verdict: **PASS_PROTOCOL_A** (IS t=3.600, N=1532, ExpR=+0.1092, p=0.00032). OOS N=77, sign positive; OOS power=0.088 (STATISTICALLY_USELESS) — OOS non-confirmatory, verdict rests on IS gates only. IS directional: Long_IS t=2.591 (n=778), Short_IS t=2.498 (n=754); neither direction clears t≥3.00 unilaterally; pooled gate applies. Theory grant: Chan Ch 7 p.155 stop-cascade at NYSE open, consistency grant with already-audited RR1.0 sibling. COST_LT12 is a friction-floor gate, not a theory-grounded filter. Allocator MAY DEPLOY; correlation gate vs existing RR1.0 sibling to be assessed at next rebalance. Does NOT qualify for PASS_CHORDIA (t < 3.79). Pre-reg: `docs/audit/hypotheses/2026-05-07-mnq-nyseopen-costlt12-rr15-chordia-unlock-v1.yaml`. Result: `docs/audit/results/2026-05-07-mnq-nyseopen-costlt12-rr15-chordia-unlock-v1.md`. [Updated 2026-05-07: OOS power disclosure + IS directional context added per code review.]

- `capital-review-ovnrng100-framing-critique-2026-05-05` — **DOES NOT SUPERSEDE `capital-review-ovnrng100-downsize-2026-05-05` below — that DOWNSIZE verdict STANDS.** This entry annotates the parent verdict with framing-critique findings. Three-revision addendum: (rev1) proposed CONDITIONAL_REMAIN_DEPLOY → auditor #1 PARTIALLY_GROUNDED, called fifth-option post-hoc rescue. (rev2) proposed UNVERIFIED → auditor #2 PARTIALLY_GROUNDED, noted UNVERIFIED's pre-reg-defined trigger ("Insufficient data to evaluate any of F1/F3/F4. DB freshness insufficient.") was NOT met since F1/F3/F4 all triggered with measured data. UNVERIFIED used with redefined meaning is the same class of taxonomy escape as CONDITIONAL_REMAIN_DEPLOY. (rev3, this version) **parent's DOWNSIZE is the procedurally correct within-taxonomy answer**. Treadmill-signal triggered per institutional-rigor.md ("more than twice = architecture is wrong"). **Findings that survive both adversarial passes as VALID CRITIQUES of pre-reg authoring discipline (not verdict overrides):** (1) parent's stated rationale partly mis-cited F3 BINDING triggers as forward-EV refutation when they are sunk-cost diagnostics — correct DOWNSIZE rationale is Carver Ch 9-10 capital-allocation-under-uncertainty (which the pre-reg did cite); (2) parent dismissed +0.097R OOS lift as "essentially base rate" when filter ExpR is 2.4x universe ExpR (artifact-inflated by N=6 off-days at pnl_r=-1.000 but not literal-base-rate). **Empirical addition:** VWAP_MID_ALIGNED 2026 fire rate 65% (49-65% range, NOT vestigial); COST_LT12 100% in 2026 per L4 row of 2026-05-01 6-lane audit (VESTIGIAL). 2 of 3 active lanes carry the vestigial class. Operational impact: ZERO. Two feedback files queued: `feedback_pre_reg_taxonomy_can_be_misframed.md`, `feedback_downsize_rationale_sunk_cost_vs_forward_ev.md`. Highest-EV follow-up unchanged from parent: OVNRNG_100 + COST_LT12 paired filter-reparametrisation pre-reg. Addendum doc (rev3): `docs/audit/results/2026-05-05-capital-review-ovnrng100-addendum.md`.
- `capital-review-ovnrng100-downsize-2026-05-05` (SUPERSEDED by addendum above) — Capital-class diligence on deployed lane `MNQ_COMEX_SETTLE_E2_RR1.5_CB1_OVNRNG_100` resolves the four findings filed by PR #234 placeholder doc. Verdict: **DOWNSIZE**. F1 VESTIGIAL: fire rate 4.3% (2019) → 91.7% (2026), 21.5x drift on absolute-points threshold (avg overnight_range 43→199 pts, 4.6x scale shift). F2 NOT_A_VIOLATION: PR #228 RECONCILED-after-FAIL is documented pre-reg specification bug (criterion 4 targeted scratch-EXCLUSIVE expectancy_r when runner reproduces scratch-INCLUSIVE strict-unlock CSV exactly, delta=0.0R) — not a no-rescue rule violation. F3 DEPLOYMENT_MATH_BROKEN: BINDING triggers — n_trials=35,616 exceeds MinBTL_max(E=1.0, 6.65yr clean MNQ)=28 by factor 1,272x; OOS power 0.07 (two-group) / 0.32 (one-sample) — STATISTICALLY_USELESS tier per RULE 3.3. CROSS-CHECK — dsr_score=0.175 vs 0.95 informational only (Amendment 2.1 demoted DSR from binding gate to cross-check until N_eff is solved). Bailey 2013 extract (line 110) literally names this lane as the only MNQ lane clearing the noise floor at the GENEROUS 16-year proxy / 500 effective trials assumption — at the actual 6.65yr/35,616-trials horizon, the lane is below noise floor. F4 LIVE_DECAY (mild): Q2 2025 +0.57R → Q3 2025 +0.29 → Q4 2025 +0.10 → Q1 2026 +0.08; live N=153 ExpR=+0.24 t=2.52 still positive but power=63% (DIRECTIONAL_ONLY tier). Verdict trace: F1 OR F3 trigger, AND F4 not outright zero/negative → DOWNSIZE per Carver 2015 Ch 9-10 capital-allocation-under-uncertainty. Allocator-side downsize implementation, per-lane decay tripwire, and generalized absolute-threshold scale-drift sweep are queued as SEPARATE follow-up stages — this audit does not push allocator state. NOT a `validated_setups.oos_exp_r` row update either (LEGACY=+0.2029 → canonical=+0.1658 requires strategy_validator path with own pre-reg). Pre-reg: `docs/audit/hypotheses/2026-05-05-capital-review-ovnrng100.yaml`. Result: `docs/audit/results/2026-05-05-capital-review-ovnrng100.md`. Stage: `docs/runtime/stages/capital-review-ovnrng100.md`.
- `chordia-pr211-post-merge-addendum-2026-05-04` — Independent evidence-auditor review of merged PR #211 (chordia queue closeout, commit `7b5b6e49`) flagged three audit-trail integrity items. Verdict (`PASS_CHORDIA` for `MNQ_COMEX_SETTLE_E2_RR1.0_CB1_X_MES_ATR60`) UNCHANGED; allocator behavior unchanged; capital impact zero. (1) IS `t_stat=4.361` was in-memory float64; CSV recompute yields `4.323` (delta=0.038, both clear 3.79). (2) OOS sign-match at `N_OOS=49` has power ≈ 0.30 — uninformative per `feedback_oos_power_floor.md`; original audit framing implied confirmatory weight, now qualified. (3) `chordia_strict_unlock_v1.py` + the audited strategy added to E2 LA registry as row #30 CLEARED — `cross_atr_MES_pct` derives from `daily_features.atr_20_pct` (rolling, prior-close, `STARTUP`), not break-bar state. Loader (`chordia.py:281-297`) silently ignores the new optional fields (`t_stat_csv_recompute`, `oos_n`, `oos_power`, `t_stat_source`). 118 drift checks pass, #134 (Chordia gate) PASS. This is audit-trail hygiene, not evidence revision; it forecloses a future-auditor confusion class without re-touching the gate.
- `target-b-vestigialness-stands-2026-05-01` — Fresh-context evidence-auditor re-verified PR #47 / PR #57 6-lane filter vestigialness claim against canonical `orb_outcomes` + `daily_features` through 2026-04-28. All 4 load-bearing claims VERIFIED (fire-rate 6/6 ≥75% — refines PR #47's "5/6" framing as L2 has shifted from 75% to 81%; lift ≤ 0R; session-driven diversification; PR #57 supersession sound). Capital action: NONE. Two doc-cleanup debts resolved in same change: `.claude/rules/quant-audit-protocol.md` line 272 cost specs corrected (MNQ $2.74 → $2.92, MES $3.74 → $3.92 to match canonical `pipeline.cost_model.COST_SPECS` post-F-4 commission fix); MEMORY.md L2-shorthand corrected (PR #57 refuted wrong-aperture framing only, NOT IS Welch p=0.014 signal). Open follow-up queued: deletion of deprecated PR #52/#54 research scripts that retain the same `orb_minutes=5` hardcode bug class as PR #189 — needs explicit user authorization before action. Audit doc: `docs/audit/results/2026-05-01-target-b-6lane-vestigialness-fresh-audit.md`.
- `work-queue-is-canonical` — `docs/runtime/action-queue.yaml` is now the canonical active-work source for meaningful open work. `HANDOFF.md` is a generated thin baton for session startup only, and local multi-terminal ownership lives in the ignored `.session/work_queue_leases.json` lease file.
- `runtime-shell-unification` — Startup orientation should consume one derived `system_brief` instead of parallel ad hoc summaries.
- `capsule-is-task-owner` — Active scoped work should carry a single work capsule with route, scope, and verification obligations.
- `history-split` — `HANDOFF.md` is current baton only; durable decisions and debt belong in dedicated ledgers.
- `pr48-conditional-edge-recovered` — The unfinished PR48 conditional-edge / multi-confluence line has been recovered from `origin/wt-codex-conditional-edge-framework`, replayed cleanly onto published `origin/main`, and re-run against canonical layers. Current truth is `CONTINUE as conditional-role / allocator shortlist`, not fresh discovery and not live-ready promotion. The next honest move is the bounded translation stage in `docs/runtime/stages/pr48-conditional-role-validation-translation.md`, not another generic confluence scan.
- `orb-cap-key-shape` — ORB-cap registry identity is `(orb_label, instrument)`, not session-only. Multi-instrument sessions must preserve per-instrument caps; callers must not collapse them back to a session key.
- `pr48-sizer-split` — PR48 frozen rel-vol sizing is no longer a pooled MES/MGC story. `MGC` is the only research-level deploy candidate from the 2026-04-23 frozen-rule replay; `MES` improved but remains `SIZER_ALIVE_NOT_READY` and must not be promoted alongside MGC.
- `pr48-translation-narrowed-to-mgc` — The bounded PR48 translation audit is now closed. Current role truth is narrower than the recovered shortlist: `MGC:cont_exec` is the only arm ready for bounded translation into a profile-local conditional surface; `MES:q45_exec` remains alive but still needs a larger bridge; `DUO:mes_q45_plus_mgc_cont_exec` and `MNQ:shadow_addon` remain `SHADOW_ONLY`. Do not reopen broad PR48 rediscovery or try to push these objects through `validated_setups` / `lane_allocator` as if they were standalone lanes. The next honest move is the exact bounded stage in `docs/runtime/stages/pr48-mgc-cont-exec-bounded-translation.md`.
- `pr48-mgc-bounded-translation-redesign` — The bounded `MGC:cont_exec` translation stage is now closed `REDESIGN`, not `IMPLEMENT`. The frozen PR48 sizer is broad (`MGC O5 E2 CB1 RR1.5`, nine sessions, both directions, per-lane rel-vol quintiles), while the only existing MGC profile-local surface is a different one-session `TOKYO_OPEN RR2.0 ORB_G4` lane. More importantly, the current runtime `size_multiplier` path is not an honest bridge: `0.5x` is a no-op at 1 contract, `1.5x/2.0x` can bypass the base lane cap after sizing, and Topstep scaling checks still project `new_contracts=1`. Do not implement `MGC:cont_exec` via the existing multiplier hook or by attaching it to the old MGC profile. The next honest move is one bounded redesign for a `shadow_only` profile-local conditional overlay contract.
- `pr48-mgc-shadow-overlay-design` — The redesign is now narrowed to one exact contract. The smallest honest next surface for `MGC:cont_exec` is a two-part `shadow_only` overlay: a checked-in static overlay spec plus a daily derived-state envelope validated against current profile/DB/code identity. Phase 1 readers should be pre-session and dashboard surfaces only. Do not use `paper_trades` as the primary carrier, do not push the overlay into `validated_setups`, and do not re-use execution-time `size_multiplier` in this stage. The active next stage is `docs/runtime/stages/pr48-mgc-shadow-only-overlay-contract.md`.
- `pr48-mgc-shadow-overlay-phase1-implemented` — Phase 1 of the `MGC:cont_exec` shadow-only overlay contract is implemented. The carrier is a static overlay spec plus a daily derived-state envelope exposed through `read_lifecycle_state()`, `pre_session_check.py`, and `live/bot_dashboard.py`. It is deliberately visibility-only: no `validated_setups`, no `lane_allocator`, no `paper_trades` carrier, no live execution sizing, and no mutation of `execution_engine.py` / `risk_manager.py`. Fail-closed/visibility coverage is in `tests/test_trading_app/test_conditional_overlays.py`, `test_lifecycle_state.py`, `test_pre_session_check.py`, and `test_bot_dashboard.py`.
- `prereg-front-door-active` — Locked preregs now have an operator front door at `scripts/infra/prereg-loop.sh` backed by `scripts/tools/prereg_front_door.py`. It makes the execution branch explicit before any run: `standalone_edge` routes to `trading_app.strategy_discovery` / `experimental_strategies`; `conditional_role` routes to a bounded runner/result-doc flow and must not auto-write into generic discovery. This is workflow hygiene only, not promotion evidence.
- `mgc-payoff-compression-scope` — The 5-minute `GC -> MGC` translation gap is actioned at the diagnostic stage: `PAYOFF_COMPRESSION_REAL` and `LOW_RR_RESCUE_PLAUSIBLE` both hold, but the rescue signal is broader `MGC` target-shape behavior, not a narrow proxy-only revival. Future follow-up must be a narrow MGC exit-shape prereg, not broad GC proxy reopening.
- `l1-europe-flow-pre-break-kill` — The restored L1 EUROPE_FLOW pre-break-context prereg was executed honestly on 2026-04-23 and closed `KILL`. `pre_velocity_HIGH_Q3` never cleared raw significance, and `rel_vol_HIGH_Q3` showed IS lift but failed `K=2` BH-FDR and flipped OOS sign. Do not reopen banned break-bar or ATR-normalized replacement variants from this path.
- `nyse-close-orbg8-kill-role-shift` — The exact `MNQ NYSE_CLOSE O5 E2 CB1 RR1.0 ORB_G8` prereg was executed on 2026-04-23 and closed `KILL`. The path is statistically strong in-sample but fails Criterion 9 era stability (`2025` on-signal `ExpR=-0.0697`, `N=132`) and provides no 2026 selector contrast (`42/42` OOS rows fire). This kills the native `ORB_G8` filter role, not the broad RR1.0 NYSE_CLOSE family. If the branch is revisited, the remaining honest question is standalone / allocator role, not another filter rescue.
- `nyse-close-role-additive-audit` — The follow-on `MNQ NYSE_CLOSE O5 E2 CB1 RR1.0 NO_FILTER` role audit was executed on 2026-04-23 against the exact 6 live MNQ lanes in `docs/runtime/lane_allocation.json`, reconstructed from canonical `orb_outcomes` + canonical filter logic. Result: `CONTINUE as allocator candidate`, not because of a rescued filter, but because the current `topstep_50k_mnq_auto` profile has a free 7th slot and adding raw `NYSE_CLOSE` improved the common-window IS portfolio math (`annualized R +9.5`, honest Sharpe `+0.136`, candidate-to-book daily corr `+0.023`). The next honest move is allocator/policy follow-through on the free-slot question, not more NYSE_CLOSE filter shopping.
- `prior-day-bridge-locks-written` — The 2026-04-22 MNQ prior-day geometry bridge package is now saved as locked exact hypotheses and canonical filter registrations (`F3_NEAR_PIVOT_50`, `PD_DISPLACE_LONG`, `PD_CLEAR_LONG`, `PD_GO_LONG`). This closes the "facts stranded in branch-local notes" problem for that path. The remaining honest move is execution / triage among already-locked paths, not another broad prior-day rescan.
- `prior-day-bridge-executed-needs-routing-closure` — The exact 2026-04-22 MNQ prior-day bridge locks were already consumed once each. Current live truth is: `F3_NEAR_PIVOT_50` exact avoid cell rejected; `PD_DISPLACE_LONG`, `PD_CLEAR_LONG`, and `PD_GO_LONG` survived on `MNQ US_DATA_1000 O5`, and `PD_CLEAR_LONG` survived on `MNQ COMEX_SETTLE O5`. Those are shelf facts, not live-routing facts: none of the promoted bridge survivors are currently in `docs/runtime/lane_allocation.json`. Do not rerun the consumed hypothesis files. The next honest move is one bounded routing/additivity audit, not more Track A discovery.
- `routing-audit-execution-translation-rule` — Routing audits must distinguish research-only additivity from executable additivity. A positive common-window add test is not a direct live-route proof when the candidate collides with an existing same-session live lane. Under the current runtime: same-aperture same-session adds are blocked by `RiskManager.max_per_orb_positions=1`, while different-aperture same-session adds require execution translation / size-down handling (`risk_manager.py` Check 7). Treat those as `KEEP_ON_SHELF` or translation-required until modeled or shadow-tested; do not label them `ROUTE_LIVE` from portfolio math alone.
- `prior-day-geometry-routing-2026-04-23` — The bounded routing/additivity audit for the five promoted MNQ prior-day geometry shelf survivors is now closed. All five remain positive shelf rows with valid current-profile static fits and broader inactive-profile relevance, but none are direct live routes today. `US_DATA_1000` rows show positive research-only additivity yet collide with the active `US_DATA_1000 O15` lane, so they require same-session execution translation before any auto-route claim. `COMEX_SETTLE PD_CLEAR_LONG` is also positive standalone but loses the same-session replacement test versus the live `COMEX_SETTLE RR1.5 ORB_G5` lane. Next honest move is not more Track A discovery; it is a narrow same-session execution-translation / shadow-test design if this branch is prioritized.
- `prior-day-geometry-translation-split` — The execution-translation pre-audit further narrows the branch. `US_DATA_1000` prior-day geometry rows are the only same-session candidates worth further auto-routing work: they are cross-aperture (`O5` vs live `O15`) and current runtime code can carry same-session different-aperture overlap with size-down handling, so a shadow-test / translation audit is honest. `COMEX_SETTLE PD_CLEAR_LONG` is same-aperture (`O5` vs live `O5`) and therefore blocked as a coexistence path by `RiskManager.max_per_orb_positions=1`; with negative replacement math versus the incumbent, it should not consume more current auto-routing capital.
- `prior-day-geometry-execution-translation-2026-04-23` — The `US_DATA_1000` same-session cross-aperture execution-translation audit is now closed. All four routed `O5` prior-day geometry candidates improve common-window portfolio math, but every candidate still trips current runtime constraints: the candidate itself is blocked (`20..36` times), same-session half-size suggestions are a complete no-op because both live and candidate rows are clamped to `max_contracts=1`, and there are small but real incremental other-live blocks (`2..4`). Incremental incumbent `O15` block deltas are `0`, so the problem is not incumbent displacement; it is that the current runtime cannot express an honest reduced-size coexistence path. Result: `ARCHITECTURE_CHANGE_REQUIRED`, not `ROUTE_LIVE`, not `KILL`. The next honest move is a dedicated shadow / policy translation stage, not more Track A discovery.
- `risk-reduction-fail-closed` — `ExecutionEngine` must not silently convert a risk-manager derisking request into a full-size entry. When `suggested_contract_factor < 1.0` would floor a 1-contract lane back to `1`, the engine now rejects the trade as `unexpressible_contract_reduction` instead of entering full-size. The prior-day geometry translation replay was rerun against that rule: candidate-side hard rejects still come from `hedging_guard` against the live `MNQ_NYSE_OPEN_E2_RR1.0_CB1_COST_LT12` lane, while the same-session `US_DATA_1000 O15` overlap now shows up honestly as incumbent-side block deltas (`85..190`) rather than silent coexistence. The branch remains `ARCHITECTURE_CHANGE_REQUIRED`.
- `mnq-parent-structure-shadow-buckets-prereg` — The next bounded current-stack follow-through is now locked in `docs/audit/hypotheses/2026-04-23-mnq-parent-structure-shadow-buckets-v1.yaml`. This is a `conditional_role` / `shadow_only` family on exact MNQ deployed-parent lane-side populations, not a broad HTF reboot, not an H01/H04 rerun, not a live sizing claim, and not a replacement for the architecture-gated `US_DATA_1000 O5 -> O15` coexistence branch. Scope is fixed to pre-entry-safe structure/context buckets only: `COMEX_SETTLE` long `PD_CLEAR_LONG`, plus `US_DATA_1000 O15` long fixed buckets from `PD_DISPLACE_LONG` and `PD_CLEAR_LONG`. Do not reopen rel_vol, calendar, `prev_week_*` / `prev_month_*`, or same-session routing claims under this prereg.
- `mnq-parent-structure-shadow-buckets-kill` — The exact bounded `mnq_parent_structure_shadow_buckets_v1` family was executed on 2026-04-23 and closed `KILL` at `K=6`. No test cleared BH-FDR. More importantly, the apparent subset-quality lift was the wrong role shape: `selected_trade_mean_r` improved in places, but `policy_ev_per_opportunity_r` fell on both exact-parent branches. This kills the exact-parent shadow-bucket role for `MNQ COMEX_SETTLE E2 O5 RR1.5 long PD_CLEAR_LONG` and `MNQ US_DATA_1000 E2 O15 RR1.5 long` fixed prior-day structure buckets. Do not reopen this under renamed score / confluence language. The surviving prior-day geometry story remains elsewhere: O5 shelf survivors plus the separately architecture-gated translation branch.
- `microstructure-gate0-is-new-data-program` — Track D remains a valid future system-upgrade branch, but not for the overclaimed reason that `bars_1m` is globally exhausted. Honest repo truth is narrower: specific 1-minute structure/participation overlays failed, while the remaining breakout-authenticity question may require an event-time order-book feature class that canonical bars cannot express directly. The cheapest falsification path should start with top-of-book event data (`MBP-1` / `TBBO`) on one exact active lane, and escalate to `MBO` only if the top-of-book gate shows real signal.
- `mes-e1-rel-vol-family-kill` — The exact locked `MES E1 O5 RR1.5 CB1 rel_vol` 20-cell family rerun is now archived as a durable `KILL`. `0/20` cells survived `BH q<0.05`, `t>=3.79`, and positive-mean floor. Several OOS cell deltas printed roughly `+0.4R` to `+0.93R`, but these are descriptive only and do not rescue the family under the prereg decision rule. Keep the `.4R` note as provenance, not promotion evidence.
- `pre-commit-advisory-scope` — `.githooks/pre-commit` no longer runs the advisory `M2.5` second-opinion scan. Commit-time gates now stop at the local truth-preserving checks (lint, format, drift, staged tests, behavioral audit, syntax). Advisory AI review is optional follow-up work, not a required save-path dependency.
- `pr48-phase2-conditional-role-native-surface-implemented` — Phase 2 of the `MGC:cont_exec` shadow-only overlay is implemented as observation only. `RoleResolver` is now a native interface for `ExecutionEngine` to record and log conditional role context (bucket IDs, frozen feature values, proposed shadow multipliers) at runtime, and live session startup wires it for profile portfolios. Shadow overlays do not mutate execution size, allocator state, `validated_setups`, or `paper_trades`. `ActiveTrade.overlay_context` and replay `JournalEntry.overlay_context` provide empirical shadow evidence for future promotion without schema or live-routing changes.
- `pr48-mes-q45-bridge-rejected` — 2026-05-11 fresh reruns of `research/pr48_role_followthrough_v1.py` and `research/pr48_promotion_shortlist_v1.py` advanced PR48 docs through canonical day `2026-05-07` and kept `MES:q45_exec` numerically positive, but the runtime bridge is rejected. The object depends on `rel_vol_<SESSION>` over `MES O5 E2 CB1 RR1.5`; `rel-vol-banned-on-e2-2026-04-28` is later and binding, so this exact q45 object cannot become a runtime overlay, profile sleeve, `validated_setups` row, or allocator route. Honest continuation requires a clean pre-entry-safe re-derivation, not a carrier for tainted rel-vol evidence. Result: `docs/audit/results/2026-05-11-pr48-mes-q45-exec-bridge-rejection.md`.
- `mnq-comex-settle-gate0-prereg-locked` — The first Track D microstructure falsification prereg is locked in `docs/audit/hypotheses/2026-04-23-mnq-comex-settle-gate0-microstructure-v1.yaml` as `DESIGN_ONLY`. It uses a `conditional_role` / `shadow_only` family (K=4) to test pre-break top-of-book imbalance (OFI, TBI, QI) on the exact `MNQ COMEX_SETTLE O5 E2 RR1.5` lane, but it is not executable until Databento top-of-book tables and a bounded runner exist. This is a falsification-first study, not an HFT deployment.
- `o-sr-debt-frame-misframed` — The HANDOFF "O-SR debt — `trading_app/live/cusum_monitor.py` implements CUSUM Eq 3, not Shiryaev-Roberts Eq 10" line is **MISFRAMED**, not a real debt. The binding Criterion 12 Shiryaev-Roberts monitor is already implemented in `trading_app/sr_monitor.py` (deployed-lane runner) plus `trading_app/live/sr_monitor.py` (score-based SR core), satisfies all four Criterion 12 parameters (50-trade pre-change baseline at `BASELINE_WINDOW`, Eq 17-18 linear-quadratic score with coefficients matching Eq 18 verbatim, ARL≈60 trading days threshold via `calibrate_sr_threshold(TARGET_ARL_DAYS)`, `lane_ctl.pause_strategy_id` as the "suspended" alarm action), and is wired through `lifecycle_state` (state envelope validation), `refresh_control_state` (direct `run_monitor` import), and `project_pulse` (state-envelope read + pulse action registration). The legacy `trading_app/live/cusum_monitor.py` is the intraday operator-alert helper used only by `PerformanceMonitor`; it is structurally a score-function CUSUM (literature passage Eq 15-16 with `z` as score, not strictly Eq 3 LLR CUSUM) and is faithful to its stated intent. Both implementations are correct as-is. Anchored verdict, anchor-by-anchor evidence, and isolated-context evidence-auditor pass at `docs/audit/results/2026-04-25-osr-debt-frame-audit.md`. Do not reopen this question without primary-source evidence that one of the four Criterion 12 parameters is actually failing.
- `rel-vol-banned-on-e2-2026-04-28` — `rel_vol_<SESSION>` is banned as a predictor on E2 entries. Numerator is `break_bar_volume` per `pipeline/build_daily_features.py:1600-1660`, so the same 41.3% post-entry-data class bug applies that already bans `break_bar_volume`/`break_bar_continues`/`break_delay_min` on E2. `.claude/rules/backtesting-methodology.md § 6.1` previously listed `rel_vol_{s}` as safe with a description ("ORB volume vs session-avg historical") that contradicted the canonical computation; doctrine drift survived the 2026-04-21 postmortem fix. § 6.1 narrowed and § 6.3 extended on 2026-04-28. Cite `docs/institutional/literature/chan_2013_ch1_backtesting_lookahead.md:20` (Chan p.4 look-ahead definition). Closes 2026-04-21 postmortem § 5.1 retro-audit. The 9 drift-check-flagged scripts are catalogued in `docs/audit/results/2026-04-28-e2-lookahead-contamination-registry.md`. Do not reintroduce `rel_vol_<SESSION>` as an E2 predictor under any framing without first verifying the canonical computation has changed.
- `late-fill-only-not-deployable-on-e2` — The `# e2-lookahead-policy: late-fill-only` annotation grammar permitted by `pipeline/check_drift.py::check_e2_lookahead_research_contamination` is statistically unsafe for any signal-discovery purpose on E2. Filtering trades to `entry_ts >= break_ts` conditions the backtest universe on a future-bar timestamp (Chan Ch 1 p.4). The retained ~59% subset is not representative of E2 entries that would fire in live trading. Acceptable use: descriptive characterization of the late-fill subset only. Forbidden use: deployment-bound conclusions or cross-cell ranking on a late-fill-filtered E2 universe. No drift-check enforcement landed for this; reviewer-judgment-only until a real consumer appears.
- `d4-aistudio-audit` — 2026-04-28: External review's structural critique of D4 Pathway B verdict audited against canonical layers + verbatim `/resources/` PDF passages. 14 claims reviewed: 4 refuted by data, 1 partly valid (RR1.0 vanity vs deployed RR1.5), 1 valid nuance (subset-not-identity for GARCH ⊂ ORB_G5), 8 confirmed-but-mechanism-grounded. **D4 verdict PARK_PENDING_OOS_POWER stands.** Carver Ch 12 p.192 verbatim addendum landed in `carver_2015_ch12_speed_and_size.md` re-framing aperture sensitivity as horizon-stop match (not microstructure fragility) when the cited mechanism operates at the implied holding-period horizon. Detail: `docs/audit/results/2026-04-28-d4-aistudio-claims-audit.md`.
- `amendment-3-2-min-trl-gate-2026-04-28` — Bloomberg-grade follow-up audit of D4 (2026-04-28) found that the bare `PARK_PENDING_OOS_POWER` label per Amendment 3.1 hides an indefinite-wait failure mode: at current cell-specific OOS accrual (~24 OOS-on/year on the D4 cell), reaching Cohen power 0.80 needs ~131 OOS-on observations = ~5.5 calendar years, NOT the implied "Q3-2026" interpretation. Amendment 3.2 added to `docs/institutional/pre_registered_criteria.md` requires every Pathway B pre-reg to compute `min_trl_years` at write time using sd_R, expected_effect_R, z_α=1.96, z_β=0.84, and classify cells as STANDARD (≤1y) / EXTENDED_PARK (≤2y) / NOT_OOS_CONFIRMABLE (>2y). NOT_OOS_CONFIRMABLE cells must choose CONDITIONAL_DEPLOY or KEEP_PARKED_INDEFINITELY at write time. Bailey-LdP 2014 grounded; closes the previously-open implementation step C3 from `docs/plans/2026-04-28-edge-extraction-phased-plan.md`. Also: Carver Ch 12 p.192 addendum (commit 25ed6f09) self-corrected to cell-specific scope after K=12 cross-session BH-FDR (best q=0.0707 at D5 framing RR1.5 BOTH-SIDES; the earlier "best q=0.391" figure was unreproducible and is retracted by 2nd-pass self-correction in the audit doc). Same self-correction surfaced D5 BOTH-SIDES conditional sizing pre-reg (`docs/audit/hypotheses/2026-04-28-mnq-comex-settle-garch-d5-both-sides-pathway-b-v1.yaml`) on deployed RR1.5 cohort (locked threshold 70, locked sizing 0.5×). **D5 classification REVISED PRE-RUN from EXTENDED_PARK → STANDARD** after empirical `sd_monthly_sharpe_diff = 0.0475` replaced rough estimate of 0.30 (independent evidence-auditor flagged the rough estimate as highest-leverage unverified number). MinTRL = 0.59y (≤1y → STANDARD). D5 backtest run not yet executed — pre-reg lock-before-run discipline.
- `d5-conditional-sizing-kill-2026-04-28` — D5 BOTH-SIDES conditional half-size backtest executed under locked pre-reg (commit 416855cd). **Verdict: KILL.** Two KILL criteria fired honestly: (1) KILL_PAIRED_P — paired t=−1.45, p=0.148 (>0.05) on per-trade-R differences (D5 reduces per-trade R by half-sizing the off-cohort; null cannot be rejected); (2) KILL_C9 — 2019 era ExpR_D5 = −0.108 < −0.05 floor (era stability fails on D5 variant). Headline numbers passed (+0.2705 abs SR_ann, +21.85% rel uplift, OOS DIR_MATCH) but the locked decision rule requires ALL KILL criteria pass. Lock-before-run discipline working as designed: had paired-p been peeked or C9 floor relaxed post-hoc, the verdict would have been rationalized to CANDIDATE_READY. Pre-reg gates locked the honesty. **D4 verdict PARK_PENDING_OOS_POWER stands; D5 sized variant DEAD; 0.5x sizing on no-edge cohort does not survive paired-significance + era-stability gates.** Detail: `docs/audit/results/2026-04-28-mnq-comex-settle-d5-both-sides-pathway-b-v1-result.md`. Runner: `research/phase_d_d5_mnq_comex_settle_d5_both_sides.py`. Pre-reg: `docs/audit/hypotheses/2026-04-28-mnq-comex-settle-garch-d5-both-sides-pathway-b-v1.yaml`.
- `e2-lookahead-2026-04-29-sweep` — Drift check `check_e2_lookahead_research_contamination` 2026-04-29 sweep added two registry rows after the 18+9 set was frozen. Row 28: `research/audit_aistudio_claims_d4.py` TAINTED — filters every numeric query (claims 5-12, 8 sites) on `daily_features.orb_COMEX_SETTLE_break_dir = 'long'` selector; same class as row 27. Doctrine grounding verified by canonical-source read: `pipeline/build_daily_features.py:288-346 detect_break()` defines `break_dir` on first close-cross; `trading_app/entry_rules.py:157-216 detect_break_touch()` fires E2 on first range-cross (wick touches count); `trading_app/outcome_builder.py:865-870` reads `break_dir` from `daily_features` and passes it to `detect_break_touch` as the side argument, so the empirical 100% entry-side ↔ `break_dir` concordance on the cohort is a build-pipeline tautology (selection on close-cross outcome at the day level), not informational equivalence. CRG `importers_of`=0 → annotate + audit-doc caveat sufficient. Row 29: `research/phase_d_d0_v2_backtest.py` annotation typo (`clean` → `cleared`). Audit-doc caveat on `2026-04-28-d4-aistudio-claims-audit.md` extended with confound disclaimer: the clean re-derivation N=839 → N=1577 confounds two effects (selector removal + direction-mix addition); Claim 6 RETRACT survives on conservative grounds (tainted evidence cannot support "GARCH dominates") but the literal "atr_20_pct > garch on clean" ranking requires a long-only-with-pre-break-direction-proxy re-run before being cited as deployment evidence; that clean-proxy run is queued as follow-up. D4 PARK_PENDING_OOS_POWER verdict survives. Plan-mode self-audit caught a tunnel-vision near-miss: I initially read the 100% direction concordance as informational equivalence (would have over-broadened the postmortem ban scope); reading `outcome_builder.py` direct showed the tautology and confirmed interpretation C (postmortem empirically grounded). Drift check 95 PASSED.
- `d6-mnq-comex-settle-garch-overlay-prereg-locked` — D6 pre-reg locked at commit `54b7b948`. Reframes the D4 GARCH>70 hypothesis from a NEW LANE candidate (which failed RULE 7 additivity and Amendment 3.2 NOT_OOS_CONFIRMABLE on gate-only accrual) to an OVERLAY ROLE on the deployed `MNQ_COMEX_SETTLE_E2_RR1.5_CB1_ORB_G5` lane. Pre-flight on canonical layers (gold.db, DB max trading_day=2026-04-26): IS lift +0.1947R (gate-on N=370 ExpR=+0.2477; gate-off N=1019 ExpR=+0.0530), Welch t=2.76 p=0.0059, IS gate-on vs zero |t|=4.07; OOS lift +0.6934R (gate-on N=37 ExpR=+0.358; gate-off N=33 ExpR=-0.335), dir_match TRUE, OOS_lift >= 0.40*IS_lift TRUE, OOS Welch t=2.61 p=0.011, C8 still GATE_INACTIVE_LOWPOWER (N=37<50). Per-year stability: 5/6 IS years gate-on positive at N>=10 (2020,2021,2022,2024,2025); zero years fail C9 (N>=50 AND ExpR<-0.05); 2023 N=10 ExpR=-0.31 below floor but excluded from C9 strict eval. Amendment 3.2 MinTRL: required N=284, IS-rate basis 5.10y, OOS-rate basis 2.37y; classification NOT_OOS_CONFIRMABLE; path chosen CONDITIONAL_DEPLOY (rejected KEEP_PARKED_INDEFINITELY because OOS evidence is already accruing positively). Theory: Carver Ch 9-10 vol-targeting + Chan Ch 7 stop-cascade mechanism (verbatim PDF extracts). Two-tier decision rule on harness-recomputed |t|: >=3.00 continue_if; [2.5,3.00) park_if; <2.5 KILL_IS_T. Phase 1 carrier mirrors `pr48-mgc-shadow-overlay-design` (no validated_setups, no lane_allocation, no paper_trades, no execution_engine/risk_manager mutation). Phase 2 size-multiplier action gated on D6 PASS via a SEPARATE Phase 2 pre-reg. Forbidden in scope: harness execution before commit_sha stamping (now stamped 54b7b948), production-code mutation, threshold changes, validated_setups insert, cross-instrument expansion. Pre-reg: `docs/audit/hypotheses/2026-04-29-mnq-comex-settle-garch-d6-sizing-overlay-pathway-b-v1.yaml`. Closes the parallel-session "wait 5.5y or ship" question by surfacing CONDITIONAL_DEPLOY as the doctrine-blessed third option.
- `d6-mnq-comex-settle-garch-overlay-park-conditional-deploy-retained` — D6 harness executed under locked pre-reg `54b7b948` and emitted verdict **PARK_CONDITIONAL_DEPLOY_RETAINED** per the pre-reg `is_t_under_3_handling` clause. Welch t=2.7631 (in [2.5, 3.0) range — below Chordia with-theory threshold but above raw p<0.05 floor); IS lift=0.1947R (>=0.10 floor PASS); positive_mean_floor PASS (gate-on +0.2477, gate-off +0.0530); C9 strict PASS (zero years fail at N>=50/ExpR<-0.05); positive years (N>=10) = 5 of 6 (2020,2021,2022,2024,2025), 2023 N=10 ExpR=-0.31 below floor but excluded from C9 strict eval. Pressure test (RULE 13) fired correctly (corr(pnl_r,pnl_r)=1.0). OOS lift=+0.6934R, dir_match TRUE, OOS_lift>=0.40*IS_lift TRUE, but C8 status remains GATE_INACTIVE_LOWPOWER (N_OOS_on=37<50). Phase 1 shadow-only carrier wiring is gated on this PASS verdict but is not a deliverable of this pre-reg; it lands in a separate Phase 1 implementation commit per the overlay-contract precedent (`pr48-mgc-shadow-overlay-phase1-implemented`). Phase 2 (live size-multiplier action) is gated on Phase 1 plus separate Phase 2 pre-reg per Amendment 3.2 stop conditions. KILL_SR_ALARM operationalized: SR monitor on `MNQ_COMEX_SETTLE_E2_RR1.5_CB1_ORB_G5` is wired today (`trading_app/sr_monitor.py`, TARGET_ARL_DAYS=60); if it fires within 60 trading days post-lock, classification reverts to KEEP_PARKED_INDEFINITELY. No production-code mutation. No `validated_setups` insert. No `lane_allocation.json` entry. No `paper_trades` carrier. Result: `docs/audit/results/2026-04-29-mnq-comex-settle-garch-d6-sizing-overlay-pathway-b-v1-result.md`. JSON sidecar: `docs/audit/results/2026-04-29-mnq-comex-settle-garch-d6-sizing-overlay-pathway-b-v1-result.json`. Pre-flight verify: `research/d6_preflight_verify.py`. Runner: `research/phase_d_d6_mnq_comex_settle_overlay_pathway_b.py`.
- `csv-companion-restore-2026-05-07` — Restored the `docs/audit/results/2026-05-07-mnq-nyseopen-costlt12-rr15-chordia-unlock-v1.csv` artifact referenced in the audit md (committed earlier in chain a35acf30/fe14ee20/535aae6b) but never staged. Pure data-file restoration, no methodology change. Closes the dangling-pointer the audit md held.
- `yordanov-crossmiss-triage-v1-2026-05-07` — **HALT — DESIGN_FLAWED**: pre-registered hit-rate metric is tautological against bucket definitions (CROSS_HIT hit-rate=100% by construction, CROSS_MISS hit-rate=0% by construction). Probe ran clean against 3,103 IS trades on 3 deployed MNQ lanes; pooled gap=99.4pp is mathematical construction, NOT an edge. Per backtesting-methodology.md RULE 12 (|t|>7 STOP) + institutional-rigor.md Rule 3 (refactor on bug pattern), no promotion / no Yordanov annotation / no confirmatory pre-reg authored. Locked kill criteria pre-committed in 2026-05-07-mnq-yordanov-crossmiss-triage-v1.yaml (commit fd7c5073). Next: USER_GATE — choose amendment-v2 (orthogonal-metric pre-reg) or shelve. Detail: docs/audit/results/2026-05-07-mnq-yordanov-crossmiss-triage-v1.md.
- `c8-allocator-gate-bug-fix-2026-05-14` — **BUG-class fix** opened as PR #286 (`fix/allocator-c8-gate`). Closes the fail-open architectural gap on the C8 OOS-status gate: `validated_setups.c8_oos_status` write path landed 2026-04-24 (`6887632f`); OVNRNG_25 deployed 2026-05-14 (`95fe44c3`) — 20 days after the column became a write target. Lane-after-column = BUG (not grandfather candidate) per `feedback_pre_impl_git_archaeology_bug_vs_grandfather.md`. Second instance of `feedback_allocator_gate_class_pattern_fail_open.md` (n=2: chordia 2026-05-01, c8 2026-05-14). Changes: `trading_app/lane_allocator.py` adds `apply_c8_gate()` mirroring `apply_chordia_gate()`; `pipeline/check_drift.py` adds Check 147 `check_lane_allocation_c8_gate`; tests add `TestC8Gate` class (12 tests). Acceptance: 132 drift PASS, 72 lane_allocator tests PASS, dry-run rebalance demotes OVNRNG_25 to PAUSE with reason `c8 gate: c8_oos_status='FAILED_RATIO'`. Capital impact: OVNRNG_25 + ORB_VOL_8K auto-PAUSE on next rebalance; both were already in user's DROP set. CI: known Windows-runner flake on `test_work_capsule` (per `feedback_ci_windows_runner_hang_test_work_capsule.md`) — local clean. Companion cleanup PR #287 retires 3 stale stage files (hypothesis-filter, allocator-gate-audit, r3-trade-window-refresh) — all already shipped in prior commits.
- `canary-contamination-harness-2026-05-30` — Built a canary contamination-detection harness proving the research pipeline can RELIABLY REJECT fake edge (guard-efficacy regime; complements the noise→edge null harness `scripts/tests/test_synthetic_null.py`). 10 Tier-1 canaries each inject a known fake-edge class and assert the canonical guard CATCHES it (`fired=True`=caught); every canary carries a detect+clean-pass positive control; canary 7 is dual-variant proving value-based T0 is load-bearing when the name-guard is defeated by renaming. Guards are CALLED, never re-encoded. Stage 2 meta static-scanner (`scripts/tools/canary_guard_coverage.py` + advisory `check_research_scans_call_guards`, Check 147) flagged **66** pre-existing research scans that read a canonical layer + filter/use-E2 but reference no guard (deliverable #3 = `docs/audit/results/2026-05-30-canary-guard-coverage-deliverable-3.md`). Stage 3 BLOCKING drift gate `check_canary_suite_green` (Check 192, cache-eligible, fast) blocks the commit if any guard stops catching its trap; `scripts/tools/canary_report.py` emits CAPITAL_READY / NOT_CAPITAL_READY (current: **CAPITAL_READY**, 10/10). Promoted `moving_block_bootstrap_p` and `t0_correlation` into import-safe `research/oos_power.py` (their prior homes `sys.exit(1)`/reassign `sys.stdout` at import — the latter was a real crash caught in full-drift verification). Prereg: `docs/audit/hypotheses/drafts/2026-05-30-canary-contamination-harness.draft.yaml` (documentary; cites Aronson Ch5/8-9 MCP, FST, DSR Amendment 3.5, Chan Ch1, BH-1995/Chordia). Deferred: Tier-2 end-to-end subprocess injection. Also healed an UNRELATED pre-existing blocker: MGC `daily_features` 2026-05-29 was missing the 15m+30m apertures (partial-day torn write); rebuilt via `build_daily_features.py`, integrity PASSED. Verification: 48 new tests, full drift NO DRIFT DETECTED (171 pass), ruff clean.
