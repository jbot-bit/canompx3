# Decision Ledger

Use this file for durable accepted decisions that should survive handoff churn.

## Current

- `runtime-shell-unification` — Startup orientation should consume one derived `system_brief` instead of parallel ad hoc summaries.
- `capsule-is-task-owner` — Active scoped work should carry a single work capsule with route, scope, and verification obligations.
- `history-split` — `HANDOFF.md` is current baton only; durable decisions and debt belong in dedicated ledgers.
- `pr48-conditional-edge-recovered` — The unfinished PR48 conditional-edge / multi-confluence line has been recovered from `origin/wt-codex-conditional-edge-framework`, replayed cleanly onto published `origin/main`, and re-run against canonical layers. Current truth is `CONTINUE as conditional-role / allocator shortlist`, not fresh discovery and not live-ready promotion. The next honest move is the bounded translation stage in `docs/runtime/stages/pr48-conditional-role-validation-translation.md`, not another generic confluence scan.
- `orb-cap-key-shape` — ORB-cap registry identity is `(orb_label, instrument)`, not session-only. Multi-instrument sessions must preserve per-instrument caps; callers must not collapse them back to a session key.
- `pr48-sizer-split` — PR48 frozen rel-vol sizing is no longer a pooled MES/MGC story. `MGC` is the only research-level deploy candidate from the 2026-04-23 frozen-rule replay; `MES` improved but remains `SIZER_ALIVE_NOT_READY` and must not be promoted alongside MGC.
- `mgc-payoff-compression-scope` — The 5-minute `GC -> MGC` translation gap is actioned at the diagnostic stage: `PAYOFF_COMPRESSION_REAL` and `LOW_RR_RESCUE_PLAUSIBLE` both hold, but the rescue signal is broader `MGC` target-shape behavior, not a narrow proxy-only revival. Future follow-up must be a narrow MGC exit-shape prereg, not broad GC proxy reopening.
- `l1-europe-flow-pre-break-kill` — The restored L1 EUROPE_FLOW pre-break-context prereg was executed honestly on 2026-04-23 and closed `KILL`. `pre_velocity_HIGH_Q3` never cleared raw significance, and `rel_vol_HIGH_Q3` showed IS lift but failed `K=2` BH-FDR and flipped OOS sign. Do not reopen banned break-bar or ATR-normalized replacement variants from this path.
- `nyse-close-orbg8-kill-role-shift` — The exact `MNQ NYSE_CLOSE O5 E2 CB1 RR1.0 ORB_G8` prereg was executed on 2026-04-23 and closed `KILL`. The path is statistically strong in-sample but fails Criterion 9 era stability (`2025` on-signal `ExpR=-0.0697`, `N=132`) and provides no 2026 selector contrast (`42/42` OOS rows fire). This kills the native `ORB_G8` filter role, not the broad RR1.0 NYSE_CLOSE family. If the branch is revisited, the remaining honest question is standalone / allocator role, not another filter rescue.
- `nyse-close-role-additive-audit` — The follow-on `MNQ NYSE_CLOSE O5 E2 CB1 RR1.0 NO_FILTER` role audit was executed on 2026-04-23 against the exact 6 live MNQ lanes in `docs/runtime/lane_allocation.json`, reconstructed from canonical `orb_outcomes` + canonical filter logic. Result: `CONTINUE as allocator candidate`, not because of a rescued filter, but because the current `topstep_50k_mnq_auto` profile has a free 7th slot and adding raw `NYSE_CLOSE` improved the common-window IS portfolio math (`annualized R +9.5`, honest Sharpe `+0.136`, candidate-to-book daily corr `+0.023`). The next honest move is allocator/policy follow-through on the free-slot question, not more NYSE_CLOSE filter shopping.
- `prior-day-bridge-locks-written` — The 2026-04-22 MNQ prior-day geometry bridge package is now saved as locked exact hypotheses and canonical filter registrations (`F3_NEAR_PIVOT_50`, `PD_DISPLACE_LONG`, `PD_CLEAR_LONG`, `PD_GO_LONG`). This closes the "facts stranded in branch-local notes" problem for that path. The remaining honest move is execution / triage among already-locked paths, not another broad prior-day rescan.
- `prior-day-bridge-executed-needs-routing-closure` — The exact 2026-04-22 MNQ prior-day bridge locks were already consumed once each. Current live truth is: `F3_NEAR_PIVOT_50` exact avoid cell rejected; `PD_DISPLACE_LONG`, `PD_CLEAR_LONG`, and `PD_GO_LONG` survived on `MNQ US_DATA_1000 O5`, and `PD_CLEAR_LONG` survived on `MNQ COMEX_SETTLE O5`. Those are shelf facts, not live-routing facts: none of the promoted bridge survivors are currently in `docs/runtime/lane_allocation.json`. Do not rerun the consumed hypothesis files. The next honest move is one bounded routing/additivity audit, not more Track A discovery.
- `routing-audit-execution-translation-rule` — Routing audits must distinguish research-only additivity from executable additivity. A positive common-window add test is not a direct live-route proof when the candidate collides with an existing same-session live lane. Under the current runtime: same-aperture same-session adds are blocked by `RiskManager.max_per_orb_positions=1`, while different-aperture same-session adds require execution translation / size-down handling (`risk_manager.py` Check 7). Treat those as `KEEP_ON_SHELF` or translation-required until modeled or shadow-tested; do not label them `ROUTE_LIVE` from portfolio math alone.
- `prior-day-geometry-routing-2026-04-23` — The bounded routing/additivity audit for the five promoted MNQ prior-day geometry shelf survivors is now closed. All five remain positive shelf rows with valid current-profile static fits and broader inactive-profile relevance, but none are direct live routes today. `US_DATA_1000` rows show positive research-only additivity yet collide with the active `US_DATA_1000 O15` lane, so they require same-session execution translation before any auto-route claim. `COMEX_SETTLE PD_CLEAR_LONG` is also positive standalone but loses the same-session replacement test versus the live `COMEX_SETTLE RR1.5 ORB_G5` lane. Next honest move is not more Track A discovery; it is a narrow same-session execution-translation / shadow-test design if this branch is prioritized.
- `pre-commit-advisory-scope` — `.githooks/pre-commit` no longer runs the advisory `M2.5` second-opinion scan. Commit-time gates now stop at the local truth-preserving checks (lint, format, drift, staged tests, behavioral audit, syntax). Advisory AI review is optional follow-up work, not a required save-path dependency.
