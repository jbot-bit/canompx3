# Handoff — 2026-05-02 session entry

**Locked:** 2026-05-01 evening
**Session goal:** drive toward more high-quality deployed trades by treating the book as a portfolio, not a queue.
**Mode:** DESIGN-FIRST. No production-code edits until sequencing is confirmed.

---

## Truth-state at handoff (verified live, not from memory)

**Live deployed book** (`docs/runtime/lane_allocation.json`, rebalance 2026-04-18):

| Active (4) | Paused (4) |
|---|---|
| MNQ_EUROPE_FLOW_E2_RR1.5_CB1_ORB_G5 | MES_CME_PRECLOSE_E2_RR1.0_CB1_ORB_G8 (regime COLD) |
| MNQ_COMEX_SETTLE_E2_RR1.5_CB1_ORB_G5 | MES_CME_PRECLOSE_E2_RR1.0_CB1_COST_LT08 (regime COLD) |
| MNQ_NYSE_OPEN_E2_RR1.0_CB1_COST_LT12 | MNQ_SINGAPORE_OPEN_E2_RR1.5_CB1_ATR_P50_O15 (allocator orb_minutes bug) |
| MNQ_TOKYO_OPEN_E2_RR1.5_CB1_COST_LT12 | MNQ_US_DATA_1000_E2_RR1.5_CB1_ORB_G5_O15 (allocator orb_minutes bug) |

**Memory said "6 deployed lanes."** Reality: **4 deployed + 4 paused**. Memory drift caught 2026-05-01 evening — update MEMORY.md `Active state` line on tomorrow's first action.

**PR #51 5 CANDIDATE_READY cells** (locked 2026-04-21, theory-grounded Fitschen + Chan, BH-FDR q<0.05, OOS/IS ≥0.86) — **none in `validated_setups`**, none deployed. Verified via DuckDB query 2026-05-01.

**Phase D**: caught up to 2026-04-30. Shadow log 69 rows on disk (uncommitted, intentional — pre-commit hook fails on stale-branch ruff errors unrelated to the CSV append).

---

## Why the simple "activate PR #51" path is wrong

Bolting 5 new candidates onto a book where **half the deployed positions are paused for known bugs** is exactly the kind of "queue thinking" Carver Ch11 warns against. The right move is:

1. **Land the allocator orb_minutes fix first** (already staged in `canompx3-allocator-fix`).
2. **Re-run allocator** on corrected O5/O15/O30 data. The 2 paused O15 lanes get true scores; possibly some get retired.
3. **Audit current 4 active lanes** for filter-vestigialness (per 2026-05-01 6-lane audit finding — 5/6 filters at ≥75% fire rate, 5/6 zero filter lift).
4. **Activate PR #51 candidates** as part of a portfolio rebalance, not a queue-add.
5. **Rebalance allocation** with all candidates + survivors of vestigialness audit visible together.

This is the **portfolio rebalance framing**, not "process a backlog."

---

## Tomorrow's plan — sequenced stages

### STAGE 0 — Memory truth-state correction (5 min)

- Update `MEMORY.md` "Active state" line: 6 → 4 deployed + 4 paused. Cite `lane_allocation.json` as source.
- No git change needed (memory file).

### STAGE 1 — Allocator orb_minutes fix lands (BLOCKING — separate worktree)

**Worktree:** `C:/Users/joshd/canompx3-allocator-fix` on `fix/allocator-orb-minutes-hardcode`
**Stage spec:** `docs/runtime/stages/fix-allocator-orb-minutes-hardcode.md`
**Status when handoff written:** branch exists at `171fd73a`, scope-locked, tests defined.

This is the gating dependency. Until this lands and merges to main, PR #51 candidate activation is wasted work — the allocator that scores them is reading O5 columns for O15/O30 strategies.

**Action:** check the allocator-fix branch state. If implementation incomplete, finish it (read the stage file, follow its acceptance criteria). If implementation complete, get it reviewed + merged.

**Definition of done for Stage 1:**
- `git log origin/main` shows the allocator fix merged.
- `python pipeline/check_drift.py` passes.
- Allocator dry-run reproduces existing O5 lane stats EXACTLY; O15 lane stats CHANGE measurably.

### STAGE 2 — Re-run allocator with corrected scoring (15 min)

After Stage 1 merges:
- Pull main into the worktree being used for allocator runs.
- Run the rebalance entry script (search for it: `grep -l "save_allocation\|generate_profile_lanes" scripts/tools/`).
- Compare new `lane_allocation.json` to current. Specifically: do the 2 paused O15 lanes resume? Do their scores match audit-doc projected values (~0.09 ExpR for the SINGAPORE_OPEN one, ~0.0708 for US_DATA_1000)?
- If projection holds: the 2 lanes resume with true scores; some lanes may shift weight; possibly some are now sub-threshold.
- If projection fails: STOP and audit the allocator fix.

### STAGE 3 — Vestigialness audit on (now-corrected) deployed book (1 session)

Per 2026-05-01 fresh-context audit: 5/6 deployed filters at ≥75% fire rate 2026, 5/6 zero filter lift, L2 lift flipped −0.032R within noise. The book is **session-diversified**, not **filter-diversified**.

For each NOW-DEPLOYED lane (after Stage 2):
- Measure 2026 fire rate. >75% = effectively unfiltered.
- Measure 2026 filter lift vs unfiltered base. <0.02R = vestigial.
- For lanes flagged vestigial: pre-register a swap to either (a) unfiltered base, or (b) replace filter with mechanism-grounded one from `mechanism_priors.md`.
- Pre-reg locks before any swap. No post-hoc "the unfiltered base looks better" without H1 declaration.

**Reference doc:** `docs/audit/results/2026-05-01-target-b-6lane-vestigialness-fresh-audit.md`.

**Definition of done for Stage 3:** decision matrix per lane (KEEP / SWAP-TO-UNFILTERED / SWAP-TO-NEW-FILTER) with pre-reg cited for any change.

### STAGE 4 — PR #51 candidate activation (1-2 sessions)

**The 5 candidates** (PR #51 body, verified 2026-05-01):

| Apt | RR | Session | N_IS | ExpR | t | q(BH) | WFE | OOS/IS |
|---:|---:|---|---:|---:|---:|---:|---:|---:|
| 15 | 1.5 | US_DATA_1000 | 1495 | +0.1063 | +3.422 | 0.0037 | 0.89 | +1.51 |
| 15 | 1.0 | NYSE_OPEN | 1545 | +0.0974 | +3.958 | 0.0008 | 1.34 | +2.64 |
| 15 | 1.0 | US_DATA_1000 | 1594 | +0.0966 | +4.037 | 0.0008 | 0.86 | +1.86 |
| 5 | 1.5 | NYSE_OPEN | 1650 | +0.0953 | +3.227 | 0.0066 | 1.33 | +0.89 |
| 5 | 1.0 | NYSE_OPEN | 1693 | +0.0807 | +3.473 | 0.0035 | 1.43 | +1.68 |

All 5 are **unfiltered base lanes**, not filter overlays.

**Activation gates per `pre_registered_criteria.md`:**

- C1, C2, C8, C9 → already implemented in `strategy_validator.py`.
- C5 (DSR — Bailey-LdP 2014) → **NOT implemented as named method**. Field `dsr_score` exists in schema but no public criterion-runner. Either: (a) wire up `_check_criterion_5_dsr` using `n_trials_at_discovery` field already in schema, OR (b) document that C5 is computed at discovery time and the schema field is the gate.
- C11 (Monte Carlo account-death) → **NOT implemented**. Need design pass: simulate N=1000 paths through the candidate's IS distribution at proposed allocation weight, count account-blow paths. Carver Ch12 vol-targeting / Kelly framing.
- C12 (Shiryaev-Roberts monitor) → **NOT implemented**. SR-procedure per Pepelyshev-Polunchenko 2015. Used for live-decay monitoring AFTER deployment, not for IS validation. May be fine to defer to post-deployment.

**Honest acknowledgment:** C5/C11 are not validator wiring — they're **design work that hasn't been done**. Stage 4 may need to split into:
- 4a. Read literature extracts for Bailey-LdP 2014 + LdP-Bailey 2018 + Carver Ch12, write design doc for C5 + C11.
- 4b. Implement C5 + C11 as validator methods. Test against the 5 PR #51 candidates as the first cohort.
- 4c. Run validator on 5 candidates. Promote those that pass to `validated_setups`.

### STAGE 5 — Portfolio rebalance with new candidates + Stage 3 swap decisions (15 min)

After Stages 2/3/4:
- All 8 (active+paused) deployed lanes have current truth-grounded scores.
- Vestigial filters swapped where pre-reg approved.
- 5 PR #51 candidates promoted to validated_setups with C5/C11 cleared.
- Run allocator. Get new `lane_allocation.json`. Diff against current.
- The diff IS the recommendation. Review with Josh before saving.

### STAGE 6 (deferred) — XFA real-capital flip ops decision

Statistically unblocked per Amendment 3.2 + CPCV PASS_ONE rank-82 portfolio bootstrap (2026-04-21). **Operations decision pending.** Needs Josh + me, one conversation. Independent of Stages 1-5.

---

## What's deliberately OUT of scope tomorrow

- New edge discovery / fresh family scans. Existing pipeline has 5 candidates queued + a vestigialness audit unaddressed. Adding new searches is queue-stuffing.
- MotherDuck MCP eval (other terminal). If it comes back ADOPT, integrate into Stage 4 tooling. If KILL or PARK, no impact on this plan.
- gold-db MCP template fix (Stage 1 of original 4-stage plan). Reduced to "fix when convenient" — not on critical path to more trades.
- Phase D shadow-log commit pattern restoration. Append landed on disk; gate eval still fires 2026-05-15.

---

## Reference / canonical sources

- `docs/runtime/lane_allocation.json` — live deployed book truth.
- `trading_app/strategy_validator.py:410` — `validate_strategy` entrypoint; criterion methods at `_check_criterion_*`.
- `trading_app/lane_allocator.py` — allocator with orb_minutes bug being fixed in `canompx3-allocator-fix`.
- `docs/institutional/pre_registered_criteria.md` — Amendment 3.2; binding criteria; no post-hoc relaxation.
- `docs/institutional/literature/bailey_lopez_de_prado_2014_deflated_sharpe.md` — DSR formula + paper citations for C5.
- `docs/institutional/literature/lopez_de_prado_bailey_2018_false_strategy.md` — burden of proof on candidate.
- `docs/institutional/literature/pepelyshev_polunchenko_2015_cusum_sr.md` — SR procedure for C12.
- `docs/institutional/literature/carver_2015_ch12_speed_and_size.md` — Kelly + position sizing for C11.
- `docs/audit/results/2026-05-01-target-b-6lane-vestigialness-fresh-audit.md` — Stage 3 input.
- PR #51 body (gh pr view 51) — 5 candidates with locked stats.

---

## Anti-bias notes carrying into tomorrow

- **Book is 4-active + 4-paused, not 6-active.** Memory drift caught today.
- **No skipping ahead to Stage 4.** Activating candidates onto a half-broken book is the queue-thinking the plan exists to avoid.
- **C5/C11 are real implementation work, not stamping.** If literature reading + design takes a session, take a session. Half-implemented gates are worse than no gates.
- **Burden of proof on candidates per LdP-Bailey 2018**, even though the IS evidence is strong.
- **Phase D gate eval is 2026-05-15** (immovable). Daily appends continue from `canompx3-phase-d2`. Do not let portfolio-rebalance work displace daily Phase D append.

---

## Stop conditions / kill switches

- If Stage 1 (allocator fix) reveals additional bugs → STOP, audit, do not paper over.
- If Stage 2 (re-run allocator) does not match audit-doc projections → STOP, the fix is wrong.
- If Stage 3 vestigialness pre-reg auto-validates "swap to unfiltered" without honest comparison → reject and re-design. The 2026-05-01 fresh-context audit found L2 lift flipped within noise — that's the kind of result that should kill a swap, not justify one.
- If Stage 4 C5/C11 design takes >1 session → split, don't rush. Half a DSR implementation is worse than calling out "candidates can't activate without C5 done first."
- If at any stage the data does not support the action → KILL. The whole plan exists to produce more *high-quality* trades, not more trades.
