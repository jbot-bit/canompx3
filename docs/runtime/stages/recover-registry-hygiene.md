---
slug: recover-registry-hygiene
classification: IMPLEMENTATION
mode: IMPLEMENTATION
stage: 1
of: 1
created: 2026-04-27
updated: 2026-04-27
task: Recover salvageable additive content from orphaned `codex/control-plane-unify` (commit `9550a668`) — registers existing canonical control-plane infrastructure (`docs/runtime/action-queue.yaml`, `pipeline/work_queue.py`, `scripts/tools/work_queue.py`, `pipeline/system_authority.py`, `pipeline/system_context.py`) into `context/registry.py` so the route resolver and rendered docs surface them. Pure additive — every referenced file already exists in main; no logic changes; renders pick up the new entries automatically.
---

# Stage 1: recover registry hygiene

scope_lock:
  - context/registry.py
  - docs/context/README.md
  - docs/context/source-catalog.md
  - docs/context/task-routes.md
  - docs/context/institutional-contracts.md
  - HANDOFF.md
  - docs/runtime/stages/recover-registry-hygiene.md

## Blast Radius

- `context/registry.py` — additive only. Three classes of change:
  1. **FALLBACK_READ_SET**: add `docs/runtime/action-queue.yaml`; alphabetize (moves `HANDOFF.md` from position 2 to last).
  2. **TASKS canonical_files / doctrine_files**: register existing files (`pipeline/system_authority.py`, `pipeline/system_context.py`, `pipeline/work_queue.py`, `scripts/tools/work_queue.py`, `docs/runtime/action-queue.yaml`) in two task manifests that route the runtime control-plane concept.
  3. **Rendered-doc body**: add `## Control-Plane Truth`, `## Control-Plane Notes`, `## Control Plane` H2 sections to `render_source_catalog_markdown()`, `render_task_routes_markdown()`, `render_readme_markdown()` — surfaces the action-queue/HANDOFF authority distinction in generated `docs/context/*.md`.
- All referenced files VERIFIED present in main: `docs/runtime/action-queue.yaml`, `pipeline/work_queue.py`, `scripts/tools/work_queue.py`, `pipeline/system_authority.py`, `pipeline/system_context.py`.
- Downstream impact: `scripts/tools/context_resolver.py`, `scripts/tools/system_context.py`, `pipeline/system_brief.py` consume `context/registry.py` via imports — additions surface in their output but break nothing (no removals, no behavior changes).
- Generated docs (`docs/context/source-catalog.md`, `task-routes.md`, `README.md`) — picked up next time `make context` (or equivalent) runs; not regenerated by this commit.

## Why

`codex/control-plane-unify` (single commit `9550a668`, 2026-04-24) declared the intent that `docs/runtime/action-queue.yaml` is canonical active-work truth and `HANDOFF.md` is a rendered baton view. The infrastructure landed via other PRs (`pipeline/work_queue.py`, `scripts/tools/work_queue.py`, `docs/runtime/action-queue.yaml` all exist in main today) but the `context/registry.py` registration of those files never made it. This commit closes that gap — registry route resolution and rendered context docs now surface the canonical control-plane files.

The other 17 files touched by `9550a668` have heavy CRLF + functional drift vs current main; not recovering them in this stage. They're separate decisions for a future session.

## Acceptance

- `python -m pytest tests/test_context/` passes (10 tests).
- `python pipeline/check_drift.py` clean.
- Pre-commit hooks pass.

## Original commit reference

- `9550a668` on branch `codex/control-plane-unify`
- Author: jbot-bit, 2026-04-24
- Title: "feat(workflow): unify queue control plane"
- Files in original commit: 18 (this recovery takes only the `context/registry.py` additive subset).
