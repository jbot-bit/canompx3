---
stage_id: dst-audit-2026-05-12
classification: AUDIT
mode: READ-ONLY (verdict doc + optional test file)
opened: 2026-05-12
worktree: .worktrees/dst-audit
branch: audit/dst-transitions-2026-05-12
base: origin/main @ 398693ea
---

# DST Asymmetric-Transition Verification Audit

## Purpose

Verify the codebase resolves DST correctly across the ~3-week asymmetric windows where
US has flipped DST but UK has not (or inverse), and that historic `orb_outcomes` data
was built using equivalent canonical logic. Re-confirms the 2026-04-07 E2 canonical-window
fix held under transition-day stress.

## Scope Lock (writes only)

- `docs/audit/results/2026-05-12-dst-transition-verification.md` (new — verdict)
- `tests/test_integration/test_backtest_live_convergence.py` (only if Stage 4 finds a gap)
- `tests/test_pipeline/test_dst_asymmetric_transitions.py` (only if Stage 1/4 finds a gap)

## Blast Radius

Read-only against `pipeline/dst.py`, `pipeline/build_daily_features.py`,
`trading_app/outcome_builder.py`, `trading_app/execution_engine.py`,
`pipeline/check_drift.py`, all DST tests, and `gold.db`. No writes to `gold.db`,
no production code edits, no schema mutation.

## Stages

- Stage 0: worktree + stage doc — IN PROGRESS
- Stage 1: canonical resolver probe matrix (Mar 8 / Mar 16 / Apr 1 / Oct 28 / Nov 9 × 12 sessions × {5,15,30})
- Stage 2: data-level verification — build-timestamp of orb_outcomes vs 2026-04-07 refactor + entry_ts >= window_end check on transition-day rows
- Stage 3: backtest split honesty — pre-state per-bucket samples, then check for cliffs
- Stage 4: live↔backtest convergence on transition days (using real bars_1m)
- Stage 5: SESSION_WINDOWS callsite audit (cosmetic vs gate)
- Stage 6: verdict MD

## Adjustments from Original Plan

1. Stage 2 reframed: orb_outcomes schema has no UTC window columns; check via `pipeline_audit_log` build dates + `entry_ts >= orb_utc_window(...).end` invariant.
2. Stage 3 pre-states sample sizes before declaring "no cliff" gate, so we know if the test has power.
3. Stage 4 sources bars from `bars_1m` not synthesized.
4. Stage 5 real-gate findings escalate to separate IMPLEMENTATION stage, no inline fix.
5. PASS-with-no-findings explicitly accepted as terminal state.

## Acceptance

- Drift check baseline: 0 failures at start AND end.
- All Stage 1 probe cells match hand-computed expected UTC ± 0s.
- Stage 2 reports build dates and 0 entry_ts violations.
- Stages 3/5 categorical only (diagnostic).
- Stage 4 convergence assertions pass on transition dates.
- Verdict MD committed.
