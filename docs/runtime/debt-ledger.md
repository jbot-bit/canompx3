# Debt Ledger

Use this file for unresolved integrity, runtime, and context debt that should remain visible until closed.

## Open Debt

- `chordia-loader-has-theory-silent-downgrade` — **Loader-inferred has_theory silently overrides prereg `filter_grounding_status.verdict=NO_THEORY_GRANT`.** Surfaced 2026-05-12 during MGC LONDON_METALS Stage 1 K=1 Mode-A revalidation (`docs/audit/results/2026-05-12-mgc-london-metals-mode-a-k1-revalidation.md`). The prereg declared `filter_grounding_status.verdict: NO_THEORY_GRANT` and `chordia_threshold_basis: Criterion 4 no-theory strict threshold (t >= 3.79)`. The runner (`research/chordia_strict_unlock_v1.py` → `trading_app.hypothesis_loader.load_hypothesis_metadata`) emitted `MEASURED loader has_theory: True` and applied **threshold 3.00** (with-theory) instead of 3.79 (no-theory). Class of risk: a future prereg whose IS t-stat falls in [3.00, 3.79) could pass spuriously under a silent has_theory upgrade. The MGC L_M case verdict (FAIL_STRICT_CHORDIA at t=2.930) survives either threshold so the conclusion is unaffected, but the loader heuristic must be audited. Fix scope: `trading_app/hypothesis_loader.py` should treat `filter_grounding_status.verdict == "NO_THEORY_GRANT"` as a HARD OVERRIDE of any heuristic-inferred has_theory, OR the runner must read the prereg's explicit `chordia_threshold_basis` string and apply 3.79 when "no-theory" appears. Audit cost: ~30 min (one file + one new unit test asserting NO_THEORY_GRANT → 3.79). Drift check candidate: assert that every result MD's `MEASURED threshold applied` matches the prereg's declared `chordia_threshold_basis` number. Not blocking active capital — current 3 deployed lanes all carry PASS_CHORDIA / PASS_PROTOCOL_A audits authored under the same loader, but their stored t-stats should be re-verified at the strict 3.79 threshold to confirm none were spuriously promoted.
- `dsr-scale-z-vs-cdf` — **DSR (Deflated Sharpe) scale drift.** `trading_app/strategy_discovery.py:434` returns a pre-CDF Z-score (`(sr - e_max_null) / sqrt(v_sr_obs)`), but `docs/institutional/pre_registered_criteria.md` Criterion 5 floor `DSR > 0.95` is a probability — Bailey-LdP 2014 Eq. 2 wraps the Z in a normal CDF `Φ[…]`. A code-stored DSR=-1.0 (Z) corresponds to probability Φ(-1.0)≈0.159, which is on a different axis from the 0.95 floor entirely. Per Amendment 2.1, DSR is cross-check only until N_eff is solved, so this is not currently a kill criterion — but every result MD that cites a DSR number is implicitly mixing scales. Class of risk: future code or doctrine that treats the stored value as a probability without CDF-wrapping. Surfaced during MGC LONDON_METALS Stage 1 prereg authorship 2026-05-12 (5 active MGC LONDON_METALS family heads carry stored Z ∈ [-1.39, -0.36] — would all "fail" 0.95 if mistakenly compared to the criterion as-is). Fix scope: either (a) wrap `dsr` return in `_norm_cdf` and rename to `dsr_prob`, OR (b) rename the field to `dsr_z` everywhere and add a separate `dsr_prob` consumer column. Either way: every reader (validator, allocator, dashboard, result MDs) needs to be audited. Files to audit: `trading_app/strategy_validator.py`, `trading_app/lane_allocator.py`, any doc citing "DSR=" or "dsr=". Stage gate: changing the field name is a schema migration; wrapping in CDF preserves the column but changes the numeric range. Pre-reg drift check should be added (sentinel: any new prereg citing DSR threshold ≥ 1.0 indicates someone wrote against Z scale not probability scale).
- `phase4-hypothesis-sha` — Phase 4 hypothesis SHA integrity debt remains an explicit unresolved item until repaired or quarantined.
- `operator-pulse-latency` — `project_pulse --fast` should stay bounded enough for frequent shell use and must avoid broad startup scans.
- `capsule-hygiene` — Work capsules need disciplined upkeep so scope, verification, and ledger refs remain accurate.
- `cost-realism-slippage-pilot` — Book-wide cost-realism debt. **MGC, MNQ, and MES routine-central-tendency portions CLOSED:**
   - MGC TBBO pilot (N=40, `research/output/mgc_e2_slippage_analysis.json`): MEDIAN=0 ticks, p95=2.05, max=263 (dominated by 2018-01-18 gap event). Trimmed mean ≈0.18 ticks. Raw mean=6.75 is outlier-driven.
   - **MNQ TBBO pilot v2 (N=142, 2026-04-20 gap fill):** MEDIAN=0 ticks, p95=0.00, max=+2, **100% of days ≤ 2 ticks across all 9 deployed sessions**. Gap fill added EUROPE_FLOW / COMEX_SETTLE / US_DATA_1000 (10 days each, clean medians). Full result: `docs/audit/results/2026-04-20-mnq-e2-slippage-pilot-v2-gap-fill.md`. Modeled friction UNCHANGED.
   - **MES TBBO pilot v1 (N=40, 2026-04-24):** MEDIAN=0 ticks, p95=0.00, max=0, **100% of days ≤ 1 modeled tick across all current deployable MES sessions** (`CME_PRECLOSE`, `COMEX_SETTLE`, `SINGAPORE_OPEN`, `US_DATA_830`). Full result: `docs/audit/results/2026-04-24-mes-e2-slippage-pilot-v1.md`. Modeled friction UNCHANGED.
   - **Phase D 2026-05-15 MNQ COMEX_SETTLE gate:** no slippage-based blocker. 10-sample COMEX_SETTLE median=0, mean=0.0.
   - MGC + MNQ + MES routine central tendency: median = 0 ticks. Honest central tendency is modeled-conservative.
   - STILL OPEN: event-day tail NOT measured for MNQ/MES routine samples (known-unknown, not refuted concern). **MES event-tail debt is now bounded** via `docs/plans/2026-04-25-mes-event-tail-slippage-debt-scope.md`. Five escalation triggers named there: (A) any MES lane enters the live allocator (session-agnostic; note two MES lanes already exist in `lane_allocation.json` `paused[]` — operator un-pause fires Trigger A with no code change); (B) MES cost-model parameters change; (C) MES `max_contracts > 1`; (D) routine MES pilot is contradicted by a second sample; (E) `HOLDOUT_SACRED_FROM` is changed (IS/OOS reclassification). Pre-registered method: per-cell repricing across 4 event classes (calendar-event, macro-shock, liquidity-degraded, microstructure-burst), `reprice_e2_entry` repricer, `orb_utc_window` boundary, governance bar = measured per-side signed median `<= 1 tick` AND p95 `<= 2 ticks` against the canonical 1-tick-round-trip modeled baseline (NOT a "5-ticks-per-side" baseline — earlier draft had a unit error reading `slippage = 1.25` dollars as points; corrected after evidence-auditor review). MNQ event-tail debt remains scoped at the same level by parity (no equivalent escalation-trigger doc authored yet — open follow-up).
- ~~`mnq-tbbo-pilot-script-broken`~~ **CLOSED 2026-04-20.** Script rewritten with canonical `reprice_e2_entry` delegation + real `orb_high/orb_low` from daily_features + new `--reprice-cache` mode. Canonical regression + caller tests at `tests/test_research/test_reprice_e2_entry_regression.py` + `tests/test_research/test_mnq_pilot_caller.py`. Ran clean against existing 119-file cache: 114/119 valid, 5 legitimate `no_trigger_trade_found` errors.
- ~~`preflight-checks-total-hardcode`~~ **CLOSED 2026-05-07.** Refactored `scripts/run_live_session.py::_run_preflight` from manual `checks_total = 6` to list-of-callables pattern (`PREFLIGHT_CHECKS: list[CheckFn]` + `PreflightContext` dataclass for shared state — `components` from `_check_auth` consumed by `_check_contracts` / `_check_notifications`, `components_all_pass` set by `_check_notifications` and read by the summary branch). `checks_total = len(PREFLIGHT_CHECKS)` is now dynamic. Companion test `tests/test_scripts/test_run_live_session_preflight.py` injects a 7th check and confirms (a) header reads `[1/7]..[7/7]`, (b) summary reads `Preflight: N/7 passed`, (c) a known-failing injected check is counted toward the total. Stage doc: `docs/runtime/stages/preflight-checks-total-hardcode.md`. Origin: PR #248 `/code-review` LOW-1 deferred follow-up.
