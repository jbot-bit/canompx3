# G6 — Holdout-Integrity Certificate — PR #48 Participation-Shape

**Gate:** G6 (holdout-policy compliance; pre-reg git SHA; evaluation-date discipline)
**Certificate status:** **PASS with RESEARCH-PROVISIONAL caveat**
**Authority:** `docs/institutional/pre_registered_criteria.md` v2.7 Amendment 2.7 + v3.1 Amendment 3.1.

## Holdout boundary

`trading_app/holdout_policy.py` `HOLDOUT_SACRED_FROM = 2026-01-01` per Amendment 2.7:
> "Holdout policy corrected to Mode A (holdout-clean). 2026-01-01 is the sacred holdout boundary going forward. The 3+ months of real-time 2026 data is the accumulating forward OOS record."

## Pre-reg discipline

- **Pre-reg file:** `docs/audit/hypotheses/2026-04-20-participation-shape-cross-instrument-v1.yaml`
- **Pre-reg commit SHA (documented in YAML line 168):** `dab4df0b`
- **IS window declared in YAML line 77:** `trading_day < trading_app.holdout_policy.HOLDOUT_SACRED_FROM (2026-01-01)`
- **OOS window declared in YAML line 78:** `not used — IS-only specification-shape audit`

This is a Mode A-compliant IS-only pre-reg. The 2026 holdout is NOT consumed by PR #48's analysis.

## Parent-chain context (prior PRs cited in pre-reg)

PR #48 is a shape RESPECIFICATION of prior universality tests:

| Parent PR | Commit | Status |
|---|---|---|
| MNQ inverted-U | `126ed6b8` | CONFIRMED under its own inverted-U spec |
| MES inverted-U threshold-test | `75b0d0bc` | NO_REPLICATION |
| MGC inverted-U | `ce78fd7e` | NO_REPLICATION |
| Q4-band deployment-shape (doc-only) | `a22930f0` | untouched |

PR #48's shape-change (monotonic-up instead of inverted-U) is a post-audit re-analysis of the same underlying IS data. This is legitimate IS analysis — it does NOT consume 2026 OOS.

## RESEARCH-PROVISIONAL classification

Per Amendment 2.7:
> "The 124 existing validated_setups are grandfathered as RESEARCH-PROVISIONAL per Amendment 2.4 — they were discovered with 2026 data in scope and are NOT OOS-clean."

PR #48 (dated 2026-04-20) is dated AFTER the Mode A sacred-holdout lock (2026-04-07). Its IS window is scoped to `< 2026-01-01`. It is therefore NOT grandfathered; it IS holdout-clean per declared scope.

However, per directive § Contamination declaration in the deploy-live pre-reg:
- PR #48 and its successors (PRs #49/#50/#51) did touch 2026 OOS data in subsequent Pathway-B / Pathway-A confirmations.
- Shadow-deploy of this shape signal thus continues an already-provisional window, not a fresh-holdout validation.
- The 2-week shadow window accumulates FRESH forward-OOS, which counts toward future fresh-holdout decisions, NOT backward toward PR #48's existing peeked record.

## Evaluation-date discipline

- Pre-reg locked: 2026-04-20.
- Observed result: 2026-04-20 (same-day run).
- Kill criteria declared pre-run (`kill_if: "β₁ ≤ 0 OR t < +3.0 OR one-tailed p ≥ 0.05"`).
- No post-hoc threshold relaxation observed in the result file vs the pre-reg.

## Audit trail

- Pre-reg in git: YES (`docs/audit/hypotheses/2026-04-20-participation-shape-cross-instrument-v1.yaml`).
- Result in git: YES (`docs/audit/results/2026-04-20-participation-shape-cross-instrument-v1.md`).
- Script in git: YES (`research/participation_shape_cross_instrument_v1.py`).
- Commit SHA pinned in pre-reg: YES (`dab4df0b`).

## Verdict

PR #48 respects the Mode A holdout boundary. Its IS-only scope is declared and enforced in the pre-reg. The shape respecification versus prior PRs is legitimate IS analysis. Shadow-deploy classification carries a RESEARCH-PROVISIONAL label per Amendment 2.7 spirit — the shadow window is fresh forward-OOS, not a fresh-holdout validation of PR #48 itself.

**G6 = PASS with RESEARCH-PROVISIONAL caveat.** Pre-reg YAML § contamination_declaration already captures this.
