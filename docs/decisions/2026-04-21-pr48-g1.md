# G1 — Timing Validity Certificate — PR #48 Participation-Shape

**Gate:** G1 (timing / look-ahead)
**Certificate status:** **PASS**
**Authority:** `.claude/rules/daily-features-joins.md` § Look-Ahead Columns; `.claude/rules/backtesting-methodology.md` RULE 1 + RULE 6.1; `pipeline.dst.orb_utc_window`; postmortem `docs/postmortems/2026-04-07-e2-canonical-window-fix.md`.

## Variables used in PR #48

**Primary feature:** `rel_vol_{s}` — ORB volume normalized vs historical session mean for that session + aperture.

**Secondary variables:**
- Entry model: E2 CB1 (stop-market at ORB level, confirm bar = 1, one-bar delay before entry).
- Aperture: 5-minute primary (secondary 15/30-minute shape-map).
- RR target: 1.5 primary (secondary RR 1.0 / 2.0).
- Target regression variable: `pnl_r` (post-trade, NOT used as a predictor — used only as the dependent variable).

## Source-bar vs decision-time-bar mapping

| Variable | Source window | Known at time | Decision (E2 entry) fires at |
|---|---|---|---|
| `rel_vol_{s}` | ORB bars (e.g. minutes 0-4 for 5m aperture) | ORB close | ORB-close + 1 bar (CB1 break confirm) |
| ORB high / low | ORB bars | ORB close | " |
| Break direction | break bar (bar immediately post-ORB) | Break-bar close | " |
| `pnl_r` (DV) | Post-entry trade lifecycle | Post-exit | N/A — used only as dependent variable |

Per backtesting-methodology RULE 6.1 § Safe (always trade-time-knowable) features:
> "rel_vol_{s} (ORB volume vs session-avg historical — known at ORB end)"

## Look-ahead columns check

RULE 6.3 Banned list: `double_break`, `*_mae_r`, `*_mfe_r`, `*_outcome`, `pnl_r` (as predictor), `*_fill_price` as predictor.

PR #48 uses `pnl_r` as DEPENDENT VARIABLE, not predictor. `rel_vol` is not in the banned list.

Window-derived features (RULE 1.2 conditional-validity): `session_asia_*`, `session_london_*`, `session_ny_*`, `overnight_*`, `pre_1000_*`. **PR #48 uses NONE of these.**

## Canonical-window derivation

Per CLAUDE.md § Volatile Data Rule:
> "ORB window timing → `pipeline.dst.orb_utc_window(trading_day, orb_label, orb_minutes)` (never derive from `break_delay_min`; never fall back to `break_ts`)"

`research/participation_shape_cross_instrument_v1.py` (PR #48 script) joins `orb_outcomes × daily_features` on `(trading_day, symbol, orb_minutes)`. ORB-minute stratification is explicit in the triple-join per backtesting-methodology RULE 9 (CTE guard).

## Holdout compliance

Pre-reg scope line 77:
> `is_window: "trading_day < trading_app.holdout_policy.HOLDOUT_SACRED_FROM (2026-01-01)"`

`HOLDOUT_SACRED_FROM` in `trading_app/holdout_policy.py` anchors at 2026-01-01 per pre_registered_criteria v2.7 Amendment 2.7.

## Smell-test tripwire

|t|=+9.589 / +11.802 / +7.541 triggers edge-finding-playbook §3 smell test (>7 assume look-ahead until proven). This cert clears the "source bar vs decision bar" part. G5 carries the fuller defence (why not look-ahead OR scale artifact given the mechanism + N).

## Verdict

PR #48 uses only features whose source-bar precedes the E2 CB1 decision bar, none of which appear in the banned look-ahead column list. Triple-join respected. Holdout window respected.

**G1 = PASS.**
