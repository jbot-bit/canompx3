# G7 — Negative-Controls Record — PR #48 Participation-Shape

**Gate:** G7 (destruction shuffle, RNG-null, positive control)
**Certificate status:** **WEAK — positive control only; destruction-shuffle + RNG-null DEFERRED to shadow-window remediation**
**Authority:** `docs/institutional/literature/bailey_et_al_2013_pseudo_mathematics.md` (selection-bias critique).

## Positive control — PASS

**Control:** per-lane agreement rate. If the signal were a pooling artefact or Simpson's paradox, per-lane agreement would approach 50% (lane-level flips mask pooled direction).

**Observed:**
- MNQ: 91.7% (22 of 24 lanes same-sign with pooled β₁ > 0).
- MES: 100.0% (22 of 22).
- MGC: 100.0% (18 of 18).

All three instruments' per-lane agreement exceeds the pre-committed ≥ 50% confirmation threshold (per pre-reg `decision_rules.verdicts_per_instrument` line 132-134). Under a null, the expected agreement is 50%. Probability of observing ≥ 22/24 or 22/22 or 18/18 under binomial(n, 0.5) is essentially zero.

**Positive control passes.**

## Destruction shuffle — NOT IN PRE-REG

Pre-reg `testing_discipline.integrity_gates` (line 123-129) lists:
- per-instrument rel_vol non-null coverage ≥ 90%.
- per-instrument pooled N ≥ 1000.
- triple-join orb_outcomes × daily_features.
- CTE reads filter orb_minutes = N-under-test (RULE 9).
- rank computed WITHIN-lane (scale-invariance).
- per-lane agreement % reported (RULE 14).

**Destruction shuffle (y-shuffle null: randomise pnl_r, run the same regression, expect t ≈ 0) is NOT present** in the integrity_gates.

## RNG-null — NOT IN PRE-REG

RNG-null (replace `rel_vol` with uniform random noise, run regression, expect t ≈ 0 uniformly) is **NOT present** in the integrity_gates.

## Why this isn't a hard fail but is a WEAK

1. The positive-control per-lane agreement is a strong-enough structural check to rule out Simpson-paradox / pooling-artefact explanations.
2. RULE 6.1 feature-safety + G1 timing-validity rule out look-ahead.
3. The mechanism (Chan Ch 7 stop cascades) predicts the observed direction and order of magnitude.
4. Sample sizes (N = 7k-18k per instrument) make coincidental |t|>7 implausible under any reasonable null.
5. However, institutional rigor requires BOTH destruction shuffle AND RNG-null per Bailey et al 2013 false-strategy selection-bias protocols. The pre-reg omitted these.

## Remediation

Pre-reg deployment YAML `g9_kill_criteria` must include, as shadow-window remediation (not pre-shadow block):

- Destruction shuffle: randomise pnl_r across the same IS cell; re-run regression; expect |t| < 2.5 with probability > 95% on 1000 shuffles.
- RNG-null: replace rel_vol with uniform(0,1) ranks; re-run regression; expect |t| < 2.5 with probability > 95% on 1000 rngs.

Both tests require local Python + read access to `orb_outcomes` pnl_r series. Not blocking Phase 6 shadow deploy (positive control already establishes non-spuriousness), but required before any promotion to real capital via C3.

## Verdict

PR #48 has a strong positive control (per-lane agreement) but is missing the standard Bailey-et-al-2013 destruction-shuffle and RNG-null.

**G7 = WEAK (positive-only).** Destruction-shuffle + RNG-null REMEDIATION tasks added to pre-reg YAML deployment block. Shadow can proceed; real-capital flip cannot until remediation complete (independent of Phase 6e monitoring gap also blocking real-capital flip).
