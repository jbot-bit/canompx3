# Hypothesis Registry — Cross-Session Momentum: COMEX_SETTLE > CME_PRECLOSE
#
# Locked:       2026-04-13T17:00:00+10:00
# Author:       Claude Code session (directional context alignment program)
# Holdout mode: A (2026-01-01 sacred)
# Data horizon: MNQ 6.65yr post Phase 3c clean real-micro
# Testing mode: individual (Pathway B, Amendment 3.0)
#
# Theory citation (LOCAL sources):
# 1. scripts/tmp/directional_context_alignment.py Phase A (Apr 13 2026):
#    Comprehensive 90-pair cross-session scan. COMEX > CME_PRECLOSE is the
#    ONLY pair with IS statistical significance (Welch p=0.034).
# 2. Stress test (same session):
#    - Year-by-year: 7/8 positive (best consistency of any pair)
#    - Direction symmetry: longs +0.125, shorts +0.126 (near-perfect)
#    - MES cross-confirms OOS +0.284
#    - OOS MNQ: +0.116 lift
# 3. Zero look-ahead: uses break-level proxy (99.7% agreement with actual
#    entry_price). Prior session ORB boundary vs current break level.
# 4. Mechanism: COMEX_SETTLE (03:30 Brisbane) establishes late-day direction.
#    CME_PRECLOSE (05:45 Brisbane) inherits or reverses. When COMEX is
#    winning and CME_PRECLOSE breaks same way = genuine continuation.
#    When COMEX is losing and same direction = doubling down on losers.

metadata:
  name: "cross-session-comex-cme-preclose"
  purpose: |
    Pre-register CROSS_COMEX_MOMENTUM filter for MNQ CME_PRECLOSE.
    Strongest fundamentals: IS p=0.034, 7/8 years, symmetric, MES confirms.
  date_locked: "2026-04-13T17:00:00+10:00"
  holdout_date: "2026-01-01"
  data_horizon_years_clean: 6.65
  data_source: "real CME Micro E-mini Nasdaq-100 futures 2019-05-06 onwards"
  testing_mode: "individual"
  bailey_compliance:
    trials: 3
    min_btl_required_years: 0.01
    status: "PASS"
  total_expected_trials: 3

hypotheses:
  - id: 1
    name: "MNQ CME_PRECLOSE CROSS_COMEX_MOMENTUM RR1.0 O5"
    filter:
      type: CROSS_COMEX_MOMENTUM
      prior_session: COMEX_SETTLE
    scope:
      instruments: [MNQ]
      sessions: [CME_PRECLOSE]
      orb_minutes: [5]
      rr_targets: [1.0]
      entry_models: [E2]
      confirm_bars: [1]
      stop_multipliers: [1.0]
    theory_citation: "Cross-session momentum — COMEX_SETTLE establishes late-day direction, CME_PRECLOSE inherits. IS p=0.034, 7/8yr positive, symmetric. Codex Round-3 memo + Apr 13 directional alignment scan."
    expected_trial_count: 1
    kill_criteria:
      - "BH FDR q=0.05 fails at K=3"
      - "Walk-forward efficiency < 0.50"
      - "OOS direction flips"

  - id: 2
    name: "MNQ CME_PRECLOSE CROSS_COMEX_MOMENTUM RR1.5 O5"
    theory_citation: "Same mechanism as H1 at wider RR target. Cross-session momentum persistence."
    filter:
      type: CROSS_COMEX_MOMENTUM
    scope:
      instruments: [MNQ]
      sessions: [CME_PRECLOSE]
      orb_minutes: [5]
      rr_targets: [1.5]
      entry_models: [E2]
      confirm_bars: [1]
      stop_multipliers: [1.0]
    expected_trial_count: 1
    kill_criteria:
      - "BH FDR q=0.05 fails at K=3"
      - "Walk-forward efficiency < 0.50"

  - id: 3
    name: "MNQ CME_PRECLOSE CROSS_COMEX_MOMENTUM RR2.0 O5"
    theory_citation: "Same mechanism as H1 at wider RR target. Cross-session momentum persistence."
    filter:
      type: CROSS_COMEX_MOMENTUM
    scope:
      instruments: [MNQ]
      sessions: [CME_PRECLOSE]
      orb_minutes: [5]
      rr_targets: [2.0]
      entry_models: [E2]
      confirm_bars: [1]
      stop_multipliers: [1.0]
    expected_trial_count: 1
    kill_criteria:
      - "BH FDR q=0.05 fails at K=3"
      - "Walk-forward efficiency < 0.50"

total_hypothesis_count: 3
