# Review 10 — cost/contract specs (cost_model.py + asset_configs.py)

- **target:** pipeline/cost_model.py COST_SPECS, pipeline/asset_configs.py; consumers account_survival.py + session_orchestrator.py
- **verdict:** CLEAR — specs match CME, friction conservative, single canonical source
- **date:** 2026-06-06
- **agent:** live-risk-auditor (a24dfdc2cba75b9e9)

---

**[CORRECT] CME contract specs match true exchange specs.**
`cost_model.py:187` MNQ `point_value=2.0` (CME $2/pt ✓), `:199` MES `5.0` ($5/pt ✓), `:104` MGC `10.0` ($10/pt ✓). Tick sizes MNQ/MES 0.25 (`:192,204` ✓), MGC 0.10 (`:108` ✓). No DD under-estimation on any live instrument.

**[CORRECT] Commissions + slippage non-zero, conservative-biased.**
MNQ commission_rt=1.42 (`:188`), slippage=1.00 (`:190`), spread_doubled=0.50 (`:189`). MES commission 1.42, slip 1.25 (`:200-202`). MGC commission 1.74, slip 2.00 (`:105-107`). MNQ Rithmic canonical $1.42 (comment `:181`) matches. TBBO pilot (N=56 lanes) measured median 0-tick slippage — modeled 1-tick RT is conservative. F-4 fix documented `:74-95`.

**[CORRECT] Single canonical source; no trading_app duplication.**
`account_survival.py:27` imports `get_cost_spec, risk_in_dollars` from pipeline.cost_model (used `:400-403, :424`). `session_orchestrator.py:23` imports `CostSpec, get_cost_spec` (used `:510-511, :2122`). `asset_configs.py` holds routing/DBN metadata only — no `point_value` field. Both the survival gate and the live orchestrator derive from the SAME source — no drift path.

**SKIPPED:** SIL/MCL (dead-for-ORB, not live); SESSION_SLIPPAGE_MULT live-only multipliers (only increase friction, cannot produce false PASS).

**RESIDUAL:** the row-03 hardcoded-1-micro bug (`account_survival.py:407`) is NOT a cost-spec bug — cost_model returns correct values, but the gate assumes 1 contract regardless. Spec correctness here does not rescue the D-3 seam.

**Decision: CLEAR** for cost/contract spec correctness. The wrong-number-mis-sizes-gate risk does NOT materialize — the numbers are right and single-sourced.
