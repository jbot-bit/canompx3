# Review 07 — live sizing path / D-3 seam (engine half)

- **target:** execution_engine.py (_compute_contracts), portfolio.py (compute_position_size_vol_scaled), session_orchestrator.py:877, prop_portfolio.py
- **verdict:** PARTIAL — clamp structurally sound at max_contracts=1 TODAY; D-3 seam is LIVE and a verified BLOCK on any clamp-lift
- **date:** 2026-06-06
- **agent:** live-risk-auditor (af7c7f89e46512285)

---

**[MED] Firm-tier `max_contracts_micro` NOT consulted at execution — per-strategy default=1 is the only live clamp.**
- `prop_portfolio.py:574-576` documents: execution clamps on PER-STRATEGY `trade.strategy.max_contracts`, NOT the firm-tier cap. `portfolio.py:86` sets `Strategy.max_contracts=1` default; `session_orchestrator.py:877` hardcodes `=1` at lane construction; `execution_engine.py:259` default param `=1`.
- `execution_engine.py:922,1140,1318` — all three entry paths call `_compute_contracts(..., trade.strategy.max_contracts)` = 1. Firm-tier cap is a DEAD constraint at execution (a second enforcement layer that doesn't enforce). LOW residual today; MED if a strategy is ever loaded with max_contracts>1 from DB without override.

**[HIGH] `compute_position_size_vol_scaled` is UNBOUNDED above max_contracts — clamp lives in the caller, not the sizer.**
- `portfolio.py:240-266` returns `int(contracts)` with NO upper bound (returns 0 on budget exhaustion, no high-side cap). `:263-266`: `contracts = available_risk / risk_dollars; if <1.0 return 0; return int(contracts)`.
- The clamp at `execution_engine.py:294-301` is the SOLE ceiling. **The clamp is the ONLY thing preventing oversize today.** Active seam: survival gate hardcodes 1-micro (row 03); live vol-sizer scales from live equity with no gate feedback. Lifting max_contracts without updating the gate = silent under-protection. CONFIRMED active D-3 seam (engine half).

**[LOW] Copy-trading passes strategy_qty verbatim to each shadow — no per-account cap re-check** (`copy_order_router.py:98-100`). LOW today (1 micro); HIGH if primary sized >1.

**No silent zero/negative path:** `portfolio.py:254-265` + `execution_engine.py:269-270` return 0 on bad inputs → REJECT event at all 3 entry sites.

**SEVERITY:** the per-strategy clamp=1 is the single structural guard today. Sound now.

**FIX SKETCH:** when clamp-lift is pursued, bound `compute_position_size_vol_scaled` output by the resolved profile cap AND wire that same value into the survival gate (closes both halves of D-3). Pass firm-tier `max_contracts_micro` into `_compute_contracts` as a hard ceiling.

**Decision: ACCEPT_WITH_RISK** today; **BLOCK any clamp-lift** until gate↔sizer parity is wired (pairs with row 03).
