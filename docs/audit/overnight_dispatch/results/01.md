# Review 01 — B[P0] bracket-leg-missing → naked position

- **target:** trading_app/live/session_orchestrator.py:2780-2821
- **verdict:** CONFIRMED
- **date:** 2026-06-06
- **agent:** live-risk-auditor (acfc1281b3bcba880)

---

**VERDICT: CONFIRMED — B[P0] finding is real and exactly as filed.**

**EVIDENCE**

The missing-leg branch (lines 2780–2794) does exactly two things after detecting a missing SL or TP:

- `log.critical(msg)` — line 2791
- `self._notify(msg)` — line 2792 (Telegram alert only; `_notify` at line 1452 is a notification-only method, never calls any close path)
- `record.bracket_order_ids = [x for x in [sl_id, tp_id] if x]` — line 2794, stores whatever partial IDs exist and continues

No `_fire_kill_switch()`, no `_emergency_flatten()`, no cancel, no flatten. Execution falls through to line 2821 (`self._stats.brackets_submitted += 1`) and the position remains open with no protective stop at the broker.

The exception branch (lines 2807–2820) when the sequential-ID fallback is also unavailable is identical: two `log.critical`/`_notify` calls, then falls through. Still no close.

Contrast with `_submit_bracket` (lines 2186–2267), which explicitly calls `_fire_kill_switch()` + `await _emergency_flatten()` on every sub-path that leaves a naked position (F4-1, F4-2, F4-3). The verify path has no such mirror.

**SEVERITY: Live-order risk / account-risk.** An adverse overnight gap or intraday spike on an unprotected futures position can breach the MLL ceiling in a single move. Highest-severity for a prop account where the consequence is account termination.

**FIX SKETCH**

Mirror the F4 pattern already in `_submit_bracket`:

1. After `self._notify(msg)` at line 2792, call `self._fire_kill_switch()` then `await self._emergency_flatten()` and `return`.
2. Same treatment for the fallback-unavailable branch at 2816–2820.
3. Remove the `# Store what we have` partial-ID store at 2793–2794 — continuing with a confirmed-missing leg is precisely wrong.
