# Review 03 — C[P1] C11 DD hardcoded 1-micro (D-3 seam)

- **target:** trading_app/account_survival.py:407, :636, :126; vs prop_profiles.py max_contracts_micro
- **verdict:** CONFIRMED (dormant today, active on clamp lift)
- **date:** 2026-06-06
- **agent:** live-risk-auditor (a67e9bdb543e344da)

---

**VERDICT: CONFIRMED. The seam is real and structural.**

**EVIDENCE**
- `account_survival.py:407` — `contracts_per_trade = 1` (literal, no param, no config read). Feeds `lots_for_position` (:408) and stamps `contracts=contracts_per_trade` on every `TradePath` (:438). All sim PnL derives from this 1-contract basis.
- `account_survival.py:636` — `contracts_per_trade_micro=1` hardcoded; comment admits "Current project daily lanes are 1-contract micro lanes per account." Not read from AccountProfile/prop_profiles.
- `SurvivalRules.contracts_per_trade_micro` (:126) exists, copied by `_with_consistency_rule` (:655), but **never consumed inside `simulate_survival`** — the Monte Carlo draws PnL from pre-built `DailyScenario` objects. It is a DEAD config knob.
- `prop_profiles.py:424` defines `max_contracts_micro` per profile (30 for 50k XFA). `account_survival.py` has ZERO reads of `max_contracts` (grep-confirmed).
- No drift check ties gate sizing assumption to live engine sizing (grep for D-3/sizing_seam/contracts_per_trade in check_drift.py → no matches).

Gate is constitutionally blind to the profile's contract limit; it will never self-correct if max_contracts is raised.

**SEVERITY: DORMANT TODAY.** Live engine clamped at `max_contracts=1`, so gate and engine agree by coincidence. Becomes ACTIVE the moment the clamp is lifted — gate still projects 1-micro DD, engine sizes N micros, gate PASS is a lie, account can bust.

**FIX SKETCH:** `_load_lane_trade_paths` accepts `contracts_per_trade: int` sourced from resolved `AccountProfile.max_contracts_micro` (or the binding vol-sizer clamp). `_build_survival_rules` reads the same value instead of hardcoding 1. Add a drift check asserting the value fed to the gate equals the value the live position-sizer uses (the seam-close).

**SKIPPED:** vol-sizer runtime clamp path in session_orchestrator.py not traced end-to-end.

**Decision: BLOCK on clamp lift until gate seam is closed.** This directly gates the clamp-lift income plan in memory.
