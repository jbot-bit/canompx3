# Review 05 — prop_profiles.py capital-routing correctness

- **target:** trading_app/prop_profiles.py (+ account_survival.py resolution, run_live_session.py classifier, check_drift.py)
- **verdict:** PARTIAL — module CLEAN on the 4 targeted checks; 1 MED deploy-readiness gap (no import-time MLL guard)
- **date:** 2026-06-06
- **agent:** live-risk-auditor (a5c5234f1ebe42256)

---

**1. is_express_funded classifier — REFUTED (clean).** `prop_profiles.py:129` default `False`. `account_survival.py:99` uses `getattr(profile,"is_express_funded",True) is False` — missing-field sentinel `True` falls to express belt (conservative). `run_live_session.py:643-646`: unknown profile_id / `prof is None` → `is_real_capital_live=True` (FAIL); `not is_express_funded` → real-capital FAIL. Drift `check_account_profiles_declare_is_express_funded` (check_drift.py:7197) AST-asserts every entry declares the field. All paths fail closed.

**2. Self-funded de-leak (bool-vs-float) — REFUTED (clean).** `account_survival.py:106`: `isinstance(self_imposed,(int,float)) and not isinstance(self_imposed,bool) and self_imposed>0`. Triple guard excludes bool subclass, rejects NaN (`>0` False), rejects zero. Stray `True` cannot become a $1 budget. `self_funded_tradovate` declares float `3_000.0` (:1065).

**3. Default safety on missing/None — REFUTED (clean).** `effective_strict_dd_budget`: anything not `is False` lands on express belt; getattr sentinel `True` → conservative treatment.

**4. Lane caps vs MLL — MED [INFERRED].** Lane caps (`max_orb_size_pts` in `DailyLaneSpec`, prop_profiles.py:76-86) are per-lane ORB-size filters, NOT dollar DD caps; no `__post_init__` validates sum-of-lane-risk ≤ tier MLL at import time. Survival gate (`account_survival.py:839`) enforces it only at preflight. A new profile with wide lanes could register silently. **SEVERITY: deploy-readiness only** — misconfigured profiles are `active=False` and survival gate is a required preflight; no blind path to live execution.

**FIX SKETCH (#4):** Add a drift check running `evaluate_profile_survival(write_state=False)` against all active profiles each `check_drift.py` pass, asserting `op_pass=True`.

**SKIPPED:** `risk_cap_pts` not a field in `AccountProfile` (lives in lane_allocation.json/allocator output, separate gate). accounts[0] routing = out of scope (P0 row 02).

**Decision: ACCEPT_WITH_RISK.** Module clean on all 4 targeted checks; residual account-risk is upstream (row 02), not here.
