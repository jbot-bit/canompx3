# Review 06 — live preflight chain (run_live_session.py)

- **target:** scripts/run_live_session.py — preflight gate chain vs the --live arm path
- **verdict:** CONFIRMED (HIGH) — preflight gates are a separate terminal mode; the --live arm does NOT run them
- **date:** 2026-06-06
- **agent:** live-risk-auditor (aeca0bf8ffdf4cbd0) + dispatcher direct code verification (agent returned only the headline claim; I verified the evidence myself before recording)

---

**VERDICT: CONFIRMED — HIGH. The 14-gate preflight chain (incl. C11/C12 survival, repo-drift, project-pulse) runs ONLY under `--preflight`, which is a terminal mode. The actual `--live` arm path bypasses it.**

**EVIDENCE (dispatcher-verified, quoted)**
- `run_live_session.py:1192` `if args.preflight:` — block ends with `sys.exit(0 if ok else 1)` at `:1219` / `:1231`. `--preflight` is a **mutually-exclusive terminal mode** that exits the process; it never falls through to arming.
- `:1234-1260` — execution-mode selection (`signal_only` / `--demo` / `--live`) is reached only when `args.preflight` is False. The `--live` branch (`:1248-1260`) does CONFIRM prompt + sets `demo=False`, then `:1267+` writes planned_launch, `:1339` acquires instance lock, `:1289/1338` constructs `MultiInstrumentRunner`/`SessionOrchestrator` and runs. **No call to `_run_preflight` anywhere on this path.**
- The only startup self-check on the arm path is the orchestrator's `run_self_tests()` — `:417-419` comment confirms it is **component probes only** (broker/bracket/fill), explicitly "non-blocking at preflight time", and is NOT the survival/drift/pulse gates. C11/C12 `evaluate_profile_survival`, repo-drift, project-pulse live exclusively inside `_run_preflight` (`:877+`, gates at `:290` C11, `:319` C12).

**SEVERITY: HIGH / account-risk.** Nothing in code forces `--preflight` to PASS before a `--live` arm. A `--live` (or dashboard auto-confirm `:1257-1258`) launch with a STALE or FAILING C11/C12 survival gate would arm anyway — the survival gate is advisory-by-omission on the real arm path. This silently undercuts rows 03/04: even with those seams fixed, the gate they protect isn't on the arm path.

**MITIGATION (operational, not enforced):** the documented operator flow runs `--preflight` first (memory: "preflight 14/14" step; START_BOT). But ordering is convention, not a code gate.

**FIX SKETCH:** On the `--live` branch, before constructing the runner, call the survival/drift/pulse gates (or `_run_preflight` in an enforce-only mode) and `sys.exit(1)` on any FAIL — making preflight a hard precondition of arming, not a separate mode. Alternatively have `SessionOrchestrator.__init__` refuse to start in `demo=False` unless a fresh PASS fingerprint exists.

**START_BOT.bat VERIFIED (dispatcher) — mitigation is FALSE.**
- `START_BOT.bat:52-53` comment claims: *"run_live_session still runs the full 14-gate preflight + CONFIRM before any order."*
- `START_BOT.bat:58` `set BOT_MODE_FLAGS=--live`; `:102` invokes `run_live_session --profile %ACTIVE_PROFILE% %BOT_MODE_FLAGS%` — i.e. `--live` with **NO `--preflight` flag**.
- Per the code trace above, `--live` without `--preflight` does NOT run the 14-gate chain. **The batch-file guarantee is documentation that contradicts the code.** This is the worst case: the operator believes preflight runs on every live arm; it does not.

**Decision: BLOCK — CONFIRMED unguarded. Escalate as the highest-value finding of the batch.** The operator-facing front-end asserts a safety guarantee the arm path does not deliver. Fix is either (a) START_BOT runs `--preflight` and aborts on non-zero before the `--live` start, or (b) `--live` enforces the gates in-process (preferred — front-ends can't be trusted to chain).
