# Review 04 — D[P1] C11 fingerprint covers only 2 files (stale-PASS / DSR-class)

- **target:** trading_app/account_survival.py:220-245 (_criterion11_code_paths); vs derived_state.py:53-64, pipeline/_dsr_policy.py precedent
- **verdict:** CONFIRMED
- **date:** 2026-06-06
- **agent:** live-risk-auditor (ac92f62eadce7d141)

---

**VERDICT: CONFIRMED — D[P1] is real; same bug class as the already-fixed DSR stale-PASS.**

**EVIDENCE**

`account_survival.py:220-225`:
```python
def _criterion11_code_paths() -> list[Path]:
    root = Path(__file__).resolve().parents[1]
    return [
        Path(__file__).resolve(),
        root / "trading_app" / "derived_state.py",
    ]
```
`:244` — `"code_fingerprint": build_code_fingerprint(_criterion11_code_paths())`. `derived_state.py:53-64` — `build_code_fingerprint` is a content hash, structurally correct but only as good as the path list.

Only 2 files hashed: `account_survival.py` + `derived_state.py`. Changes to `prop_profiles.py` (lane caps, DD budget, `is_express_funded`), `cost_model.py` (cost specs used in `apply_tight_stop`), `session_orchestrator.py` (bracket/fill), or `topstep_scaling_plan.py` (lot caps) produce ZERO fingerprint change → a cached PASS from before those edits reads as valid.

DSR-class verbatim: `pipeline/_dsr_policy.py:1-16` documents the identical failure (`file_deps:[]` served stale PASS); fix there was extracting canonical logic into a dep-listed file. `account_survival.py` never got that treatment.

**SEVERITY: live-order / account-risk.** Stale C11 PASS gates `run_live_session.py` preflight [3]. If prop_profiles cap values or cost specs change post-cache, the gate reports green on superseded params. The `is_express_funded` telemetry waiver also lives in prop_profiles.py — an undetected profile change could silently alter which capital tier the budget is computed against. **Window is live TODAY given active C11 cap-wiring work.**

**FIX SKETCH:** Expand `_criterion11_code_paths()` to every file materially affecting the survival sim: `prop_profiles.py`, `pipeline/cost_model.py`, `topstep_scaling_plan.py`, conditionally `session_orchestrator.py`. Mirror `_dsr_policy.py` — extract changeable thresholds/constants into a dep-listed canonical module so the hash is bound to the policy, not just the runner.

**SKIPPED:** preflight gate [3] not traced — can't confirm mismatch hard-blocks vs warns.

**Decision: BLOCK — do not arm live until fingerprint covers full capital-relevant dep surface.**
